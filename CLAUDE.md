# Birko Framework

Modular .NET framework with data access, communication, AI, and model infrastructure. General-purpose across enterprise back-office, e-commerce, presentation/CMS, desktop, IoT, and real-time domains.

See also:
- [CLAUDE-projects.md](CLAUDE-projects.md) — Full project catalog
- [CLAUDE-maintenance.md](CLAUDE-maintenance.md) — Maintenance guidelines, new project checklist, solution registration
- [CHANGELOG.md](CHANGELOG.md) — Historical architectural changes
- [README.md](README.md) + [docs/](docs/) — User-facing documentation

Each project has its own `CLAUDE.md` at `../Birko.{ProjectName}/CLAUDE.md` with project-specific details.

## Architecture

### Store Hierarchy (Template Method Pattern)
```
AbstractStore -> AbstractBulkStore (sync)
AbstractAsyncStore -> AbstractAsyncBulkStore (async)
```

Stores use lazy-init: CRUD methods auto-call `Init()`/`InitAsync()` before first use (via `EnsureInitialized`/`EnsureInitializedAsync` with double-checked locking). Concrete stores override `*Core` methods (e.g., `CreateCoreAsync` instead of `CreateAsync`). Public methods are `virtual` on the base class.

### SQL Stores
```
DataBaseStore<DB,T> -> DataBaseBulkStore<DB,T> (sync)
AsyncDataBaseStore<DB,T> -> AsyncDataBaseBulkStore<DB,T> (async)
```

### Repository Hierarchy
```
AbstractRepository -> AbstractBulkRepository (sync)
AbstractAsyncRepository -> AbstractAsyncBulkRepository (async)
```

### Settings Chain (Birko.Configuration)
```
ISettings (GetId)
  -> Settings (Location, Name)
    -> PasswordSettings (+Password)
      -> RemoteSettings (+UserName, +Port, +UseSecure)
        -> SqlSettings (+CommandTimeout, +ConnectionTimeout, abstract GetConnectionString)
          -> MSSqlSettings (+MultipleActiveResultSets, +TrustServerCertificate)
          -> MySqlSettings (+BulkInsertBatchSize)
          -> PostgreSqlSettings (+UseBinaryImport)
          -> TimescaleDBSettings (+TimeColumn, +ChunkTimeInterval)
    -> SqLiteSettings (+CommandTimeout, Path, GetConnectionString) — extends PasswordSettings
    -> CosmosDB Settings (+PartitionKeyPath, +RequestTimeout, +AllowBulkExecution, GetCosmosClientOptions)
    -> RavenDB Settings (+RequestTimeout, CreateDocumentStore)
    -> MongoDB Settings (+AuthDatabase, +ReplicaSet, GetConnectionString) — already existed
    -> RedisSettings (+Database, +KeyPrefix, GetConnectionString) — already existed
```

### Dependency Flow
```
Birko.Contracts (zero deps: ILoadable, ICopyable, IDefault, ITimestamped, IGuidEntity, ILogEntity, RetryPolicy)
  -> Birko.Configuration (Settings hierarchy, namespace Birko.Configuration)
  -> Birko.Data.Core (AbstractModel, ViewModels, Filters, Exceptions)
    -> Birko.Data.Stores (store interfaces, imports Configuration)
      -> Birko.Data.Repositories

Birko.Models.Contracts (zero deps: ICatalogItem, IPriceable, IVariantable, ICategorizeable, IBatchable, ILocatable, IHierarchical, IDocument, IContactable, IAddressable)
  -> Birko.Models (AbstractPercentage, AbstractTree, ValueData + Value Objects: Money, MoneyWithTax, Percentage, PostalAddress, Quantity)
    -> Birko.Models.Inventory / .Pricing / .Customers / .Users / .Product / .Category / .SEO (clean, no SQL attrs)
    -> Birko.Models.SQL (ModelMap<T>, IModelMapping<T>, ModelMapRegistry — fluent SQL mapping framework only, no canonical mappings)
      -> Birko.Models.Users.SQL / .Customers.SQL / .Inventory.SQL / .Pricing.SQL / .Product.SQL
         (one optional sibling per domain — pre-built IModelMapping<T> for User/Role/Tenant, Address/Customer,
          StockItem/StorageLocation/InventoryDocumentLine, Currency/Tax/PriceGroup, MeasureUnit/UnitConversion/ProductPartnerCode)

Birko.Time.Abstractions (zero deps: IDateTimeProvider, SystemDateTimeProvider, TestDateTimeProvider)
  -> Birko.Time (calendars, working hours, time zones)

Birko.Data.Patterns + Birko.Data.Tenant + Birko.Time.Abstractions
  -> Birko.Data.Composition (StoreWrapperBuilder — runtime decorator chains)

Birko.Data.Core
  -> Birko.Data.Tagging (ITaggable, Tag, EntityTag, ITagService, TagServiceBase)

Birko.Data.Patterns (FieldType, FieldDescriptor, ISchemaBuilder, ICollectionBuilder, IIndexBuilder, IIndexManager, IndexDefinition, ISoftDeletable, IAuditable, ISpecification, IUnitOfWork, PagedResult)
  -> Birko.Data.Migrations (IMigrationContext, IDataMigrator, IContextualMigration, IMigration, IMigrationRunner, IMigrationStore)
    -> Birko.Data.Migrations.SQL (SqlMigrationContext — reuses AbstractConnector), .MongoDB, .ElasticSearch, .RavenDB, .CosmosDB, .InfluxDB, .TimescaleDB

Birko.AI.Contracts (zero deps: ILlmProvider, Message, ContentBlock, Tool, AgentOptions, LlmProviderFactory)
  -> Birko.AI (LlmProviderBase, Agent base, AgentFactory (registration-based), default tools)
    -> Birko.AI.Providers (Claude, OpenAI, Gemini, Ollama, AzureOpenAI, etc. + ProviderRegistration)
    -> Birko.AI.Agents (CodingAgent, language agents, media agents + AgentRegistration)
    -> Birko.AI.Orchestration (ITaskDispatcher, ImplementationPlan, StepDependencyAnalyzer)
  -> Birko.AI.Resilience (ProviderRateLimiter, ProviderCircuitBreaker, CostTrackingService, TrackedLlmProvider)

Birko.Communication.OAuth (IOAuthClient, OAuthClient, OAuthSettings)
  -> Birko.Communication.OAuth.Providers (GitHubOAuthProvider — pre-configured device flow)

Birko.Communication.GraphQL (IGraphQLClient, GraphQLClient, GraphQLSettings — queries, mutations, subscriptions over HttpClient + ClientWebSocket)

Birko.Communication.gRPC (GrpcSettings, GrpcChannelPool, GrpcClientFactory, GrpcAuthenticationInterceptor, GrpcException — client over Grpc.Net.Client)
  -> Birko.Communication.gRPC.Server (GrpcServerSettings, AddBirkoGrpc, GrpcServerAuthenticationInterceptor — server over Grpc.AspNetCore; mirrors REST / REST.Server split)

Birko.BackgroundJobs (IJobQueue, JobDescriptor, RetryPolicy, JobProcessor, JobScheduler)
  -> 8 backends: .SQL, .ElasticSearch, .MongoDB, .RavenDB, .JSON, .XML, .Redis, .CosmosDB

Birko.Workflow (WorkflowBuilder, WorkflowEngine, guards, actions, Mermaid/DOT)
  -> 7 backends: .SQL, .ElasticSearch, .MongoDB, .RavenDB, .JSON, .XML, .CosmosDB
```

### Reference Implementations
- **ElasticSearch** store — reference for async/bulk operations
- **JSON** store — reference for file-based storage
- **XML** store — reference for file-based storage with `System.Xml.Serialization` (note: no native `Dictionary` support — use wrapper types)
- **InMemory** store (`Birko.Data.InMemory`) — simplest possible store (thread-safe `ConcurrentDictionary`, no persistence); the canonical test double / prototyping backend

## Usage in Consumer Solutions

When using Birko.Framework projects in your solution, create **one or more aggregator library projects** that bundle the `Birko.*` shared projects you need (e.g. `FisData.Birko`, `Symbio.Birko`, or split by layer like `{Solution}.Birko.Core` + `{Solution}.Birko.Edge` + `{Solution}.Birko.Ai`). Your other projects reference the aggregator(s) instead of importing `.projitems` directly. Default to a single aggregator; split only when concrete pain shows up — bloated binaries, leaky transitive deps, or unused-heavy-dependency pull-ins (camera, AI, hardware). This avoids compilation and transitive reference issues that arise when multiple projects import overlapping sets of shared projects independently.

Use `$(BirkoSrc)` (resolved from a root `Directory.Build.props`) for all `Import Project="…\Birko.X\Birko.X.projitems"` paths instead of hard-coded absolutes. The property reads `/p:BirkoSrc=…` first, then the `BIRKO_SRC` environment variable, then defaults to the `Birko\Framework` checkout relative to the consumer repo (the recommended layout nests the framework under a `Birko\Framework` bucket with consumers under a sibling `Birko\Consumers`, so the default resolves `..\..\Framework`; flat-sibling checkouts would use `..`). TypeScript bundlers consuming `Birko.Web.*` sources resolve a **separate `Birko\Web` bucket** — the frontend libs (`Birko.Web.Core` / `.Components` / `.Shell`) live there, apart from the .NET `Birko\Framework`. Their `build.js` walks up to find `Birko\Web` (or honors `BIRKO_SRC`). So the two builds resolve sibling buckets: `Birko\Framework` for MSBuild, `Birko\Web` for esbuild. See [README — Usage in Consumer Solutions](README.md#usage-in-consumer-solutions) for the full pattern.

## Conventions
- All stores implement: `IStore`, `IAsyncStore`, `IBulkStore`, `IAsyncBulkStore`
- All repositories implement: `IRepository`, `IAsyncRepository`, `IBulkRepository`, `IAsyncBulkRepository`
- Bulk stores support filter-based Update/Delete: `Update(filter, PropertyUpdate<T>)`, `Update(filter, Action<T>)`, `Delete(filter)`
- Use `PropertyUpdate<T>` for native platform operations (SQL SET, MongoDB $set, ES UpdateByQuery); use `Action<T>` for complex mutations
- New platform stores should override `Update(filter, PropertyUpdate<T>)` and `Delete(filter)` for native performance
- On a bulk store, `Read(filter)` returns the **collection** (`IEnumerable<T>`), not a single entity: the bulk `Read(filter, orderBy, limit, offset)` overload hides the inherited single-result `Read(filter)` from member lookup (C# only considers the most-derived type that declares the method name). Use `ReadFirst(filter)` / `ReadFirstAsync(filter)` (on `IBulkReadStore<T>` / `IAsyncBulkReadStore<T>`) for a single result, or cast to `IReadStore<T>` / `IAsyncReadStore<T>`
- Concrete stores override `protected *Core` methods (e.g., `CreateCoreAsync`, `ReadCore`), **NOT** the public CRUD methods. The base class handles lazy-init in the public wrapper
- Use protected setters for properties that derived classes need to modify
- `RemoteSettings` should be passed via `base.SetSettings()`, not constructed inline

## Code Style
- **Guard clauses:** Use early returns instead of wrapping entire method bodies in if blocks. Prefer `if (x == null) return;` over `if (x != null) { ... }`.
- **No nullable warnings:** All new code must compile without CS8600–CS8605, CS8618, CS8625. Use proper null checks, `!` only when provably safe, or `?` annotations.

## Testing
- All test projects use **xUnit + FluentAssertions**
- Every new public functionality must have corresponding tests in `Birko.{ProjectName}.Tests`
- Test both success and failure cases; include edge cases and boundary conditions
- Each test project has its own `CLAUDE.md` describing scope and conventions
- See [CLAUDE-maintenance.md](CLAUDE-maintenance.md) for test requirements on new projects and health check patterns

## Recent Updates

For older entries, see [CHANGELOG.md](CHANGELOG.md).

### Code-review remediation — low findings, Communication cluster sub-batch A (EPIC-014 / STORY-027) (2026-07-14)
Fourth STORY-027 batch (Communication cluster, worked in sub-batches). **Sub-batch A: CR-L041 … CR-L047** — Birko.Communication core + .Bluetooth. All closed. [tasks/EPIC-014/STORY-027](tasks/EPIC-014-code-review-remediation/STORY-027-low-findings/STORY.md).
- **Core (Birko.Communication)** — **L041**: `PortSettings.GetID` prefix typo `AbstratPort` → `AbstractPort` (no consumer relied on the misspelling). **L042**: `AbstractPort.InvokeProcessData` made `protected` (public→protected) — it's fired internally by a derived port after processing data, not part of the `IPort` contract for external callers (all callers are subclasses). **L043**: `IPort` now extends `IDisposable` and `AbstractPort` implements `virtual Dispose()` → `Close()` with a `_disposed` guard, so consumers get `using` support; the three ports that already had a `Dispose()` (Serial, NfcReaderPort, BluetoothLE) became `override`. **L044**: new **Birko.Communication.Tests** project (git-initialized + registered in `.slnx`/`.code-workspace`) — 6 hardware-free tests over `PortSettings.GetID`, the `ReadData` buffer helpers, the Subscribe/Invoke/UnSubscribe wiring, and `Dispose`→`Close`, via a trivial in-memory `AbstractPort` subclass.
- **Bluetooth (platform-gated)** — **L046**: the background read worker surfaces a fault via a new `ReadError` event instead of a bare `catch { break; }` that silently killed the loop. **L045** (WinRT, code-review-only): device discovery keys `discoveredDevices` by `args.Id` (present on both Added and Updated) instead of re-reading `System.Devices.Aep.DeviceAddress` from the update, which a `DeviceInformationUpdate` doesn't reliably carry. **L047** (Linux P/Invoke, compile-checked with `DefineConstants=LINUX`): `connect` uses `(uint)Marshal.SizeOf<SockaddrL2>()` (the `sizeof` operator on a user struct needs `unsafe`) and the interop structs carry `[StructLayout(LayoutKind.Sequential)]` (`BluetoothAddress` with `Pack=1`).
- Suites green: Birko.Communication.Tests 6; Bluetooth/Hardware/NFC test projects build clean (incl. the Bluetooth Linux branch). STORY-027 now **47/418**.

### Code-review remediation — low findings, Caching cluster (EPIC-014 / STORY-027) (2026-07-14)
Third STORY-027 batch: the **Caching cluster CR-L034 … CR-L040** (7 findings across Birko.Caching / .Hybrid / .Redis). Verify-first. All closed. [tasks/EPIC-014/STORY-027](tasks/EPIC-014-code-review-remediation/STORY-027-low-findings/STORY.md).
- **Type-mismatch + batching bugs** — **L036**: `MemoryCache.GetAsync` read a value back via the unchecked `(T)entry.Value!` cast, throwing `InvalidCastException` when a key was written with one type and read with an incompatible `T`; it now degrades to a Miss via `entry.Value is T typed` (a stored null is still a hit). **L040**: `RedisCache.RemoveByPrefixAsync` deleted each SCAN-matched key with an individual `KeyDeleteAsync` round-trip; it now buffers into 512-key batches and flushes via the `KeyDeleteAsync(RedisKey[])` array overload (the `CancellationToken` was already observed under CR-M034).
- **Cancellation + dispose hardening** — **L034**: the six `MemoryCache` async CRUD methods (Get/Set/Remove/Exists/RemoveByPrefix/Clear) now call `ct.ThrowIfCancellationRequested()` on entry (GetOrSetAsync already honored it). **L035**: largely resolved by CR-M030 (EvictExpired no longer sweeps `_locks`, so it can't touch a `SemaphoreSlim` that Dispose is racing to dispose); added a `volatile _disposed` flag + a guard at the top of EvictExpired as belt-and-suspenders.
- **Docs / test-gaps** — **L038**: documented that the L2-hit L1-population caps staleness at `L1DefaultExpiration` (differs by design from GetOrSet threading the caller's options). **L037**: added sliding-expiration + `CacheSerializer` round-trip tests (the GetOrSetAsync single-factory stampede and L1Max capping were already covered; NeverRemove-in-EvictExpired is left uncovered — a NeverRemove entry is still removed by any Get/Exists read, so it's not observable without reflection). **L039**: `GetL1Options` made `internal` + a full matrix test (null / absolute-below-max / absolute-above-max / sliding-only / null-max).
- Suites green: Caching 40, Hybrid 39, Redis 12. STORY-027 now **40/418**.

### Code-review remediation — low findings, BackgroundJobs cluster (EPIC-014 / STORY-027) (2026-07-14)
Second STORY-027 batch: the **BackgroundJobs cluster CR-L014 … CR-L033** (20 findings — core + all 8 backends). Verify-first. All closed. [tasks/EPIC-014/STORY-027](tasks/EPIC-014-code-review-remediation/STORY-027-low-findings/STORY.md).
- **Overflow/silent-success bugs** — **L014**: `RetryPolicy.GetDelay` computed `(long)Math.Pow(2, attempt-1) * BaseDelay.Ticks`, which overflowed to a **negative** tick count for a large BaseDelay/attempt; a negative TimeSpan slipped past the `> MaxDelay` clamp and scheduled the retry in the past. Now computed in `double` and saturated at MaxDelay. **L019**: `JobExecutor`'s typed reflection path awaited the invoked `ExecuteAsync` only `if (task != null)`, so a matched method returning a null Task was silently reported **Succeeded** without running — now returns `JobResult.Failed`.
- **RetryPolicy fallback divergence (L025 JSON, L029 RavenDB)** — both backends' `FailAsync` compared `AttemptCount < model.MaxRetries` directly, so a job with `MaxRetries == 0` went straight to Dead and the queue's injected `RetryPolicy.MaxRetries` was never read. Now `var maxRetries = model.MaxRetries > 0 ? model.MaxRetries : _retryPolicy.MaxRetries`, mirroring the reference `InMemoryJobQueue`.
- **Cosmos dequeue (L020/L021/L022)** — added a `ScheduledAt != null` guard (malformed-state safety under the Cosmos SQL translation) and a `ThenBy(EnqueuedAt)` FIFO tiebreaker for equal-priority jobs (matching MongoDB); `FailAsync(string? error = null)` tightened to the interface's non-nullable `string error`.
- **Clock + cleanup** — **L017**: `InMemoryJobQueue.EnqueueAsync` stamps `EnqueuedAt` from the injected `IDateTimeProvider` (the descriptor default escaped the clock, breaking test determinism of the `ThenBy(EnqueuedAt)` ordering). **L023/L027**: removed the dead+misleading ES `IndexName` const and Mongo `CollectionName` property (neither matched the real index/collection name). **L031**: the Redis lock-release Lua script (copy-pasted across `ReleaseAsync`/`DisposeAsync`/`Dispose`) is now a single `const` + shared sync/async release helpers.
- **Docs / comments** — **L015** (`JobStatus.Failed` documented as reserved/unused: retry→Scheduled, exhausted→Dead), **L024/L030** (ES/RavenDB schema `EnsureCreatedAsync` doc-comments corrected — they are optional pre-provisioning utilities, not the runtime path; the store lazy-inits on first CRUD), **L026/L033** (documented the intentional JSON-metadata default serializer and that the XML model relies on the pipeline resolving `Delay`→`ScheduledAt` before enqueue). **L016**: fixed the misleading "re-enqueue" shutdown comment; a true no-retry requeue is deferred (needs a dedicated `IJobQueue.RequeueAsync` — every backend `EnqueueAsync` is an insert, so re-enqueuing the same id PK-conflicts).
- **Verify-first (already resolved / not-a-defect)** — **L018** (`JobSerializationHelper` is used by the backend models; the finding was scoped to a core-only checkout), **L028** (`Birko.BackgroundJobs.MongoDB.Tests` already exists — CR-M022), **L032** (`SqlJobLockProvider` already dispatches per dialect — Postgres/MSSql/MySQL/else — under CR-M027, no longer hardcoded Postgres).
- New regression tests: RetryPolicy overflow boundary (53/100/1000/int.MaxValue), JobExecutor null-Task→Failed, InMemory EnqueuedAt-from-clock, JSON + RavenDB MaxRetries=0→policy-fallback. Suites green: core 77, JSON 7, RavenDB 7, Cosmos 3, ES 6, Mongo 6, Redis 7, XML 7. STORY-027 now **33/275**… i.e. **33/418**.

### Code-review remediation — low findings, Birko.AI cluster (EPIC-014 / STORY-027) (2026-07-14)
Started STORY-027 (the 418 **low** findings) with the first batch: the Birko.AI cluster **CR-L001 … CR-L013** across `Birko.AI` / `.Agents` / `.Contracts` / `.Providers` / `.Resilience`. Verify-first (unverified reviewer claims). All 13 closed. [tasks/EPIC-014/STORY-027](tasks/EPIC-014-code-review-remediation/STORY-027-low-findings/STORY.md).
- **Streaming fallback safety (L001)** — `Agent.RunWithHistoryAsync` snapshots the conversation before the streaming attempt and restores it before the sync fallback, so a failure part-way through streaming (which appends assistant/tool turns) no longer resubmits a dangling assistant turn (tool_use with no matching tool_result) that providers reject.
- **Dead-tuple cleanup (L002/L003)** — the `Done ? conversation : conversation` no-op ternary is gone and `HandleResponse`/`HandleToolUse` now return `bool?` (stop/continue) instead of the never-consulted `(Done,Continue)?` tuple. L003 partial: removed the redundant all-errors **reflection** in `HandleToolUse` (now an `errorCount` tracked in the tool loop); the shared cross-file `ToolResult` record was deliberately **not** introduced — the anonymous `{type,tool_use_id,content}` shape is JSON-serialized to the Anthropic/OpenAI wire with those exact snake_case keys, so a record would rename the serialized properties and risk breaking every provider (no live-provider test to catch it) — disproportionate for a low cleanup.
- **Thread-safety (L004/L009)** — `AgentRegistration.RegisterAll` and `ProviderRegistration.RegisterAll` use double-checked locking so a concurrent first-call registers exactly once (the XML docs promised "safe to call multiple times", true only for sequential calls before).
- **Config robustness (L005/L006)** — `AgentOptions.FromDictionary` uses `TryParse` throughout (a malformed value like `maxIterations="ten"` is skipped, not a `FormatException` out of an otherwise-tolerant factory); `LlmProviderFactory`'s registry is a `ConcurrentDictionary` (registration racing with reads is now well-defined).
- **Tool-arg structure (L011)** — `ClaudeProvider.ParseResponse`, the Claude streaming tool-input parse, and `GeminiProvider.ParseResponse` now deserialize the whole tool `input`/`args` object into `Dictionary<string,object>` (nested values stay `JsonElement`), so nested JSON structure survives when the Input is re-serialized to send tool results back — instead of per-property `ToString()` double-encoding objects/arrays into raw JSON strings.
- **Resilience (L010/L012/L013)** — stale default Claude model `claude-3-5-sonnet-latest` → `claude-sonnet-4-6` (the `-latest` alias 404s); `CostTrackingService.CheckBudgetAsync` short-circuits to within-budget when `_config.Enabled` is false (was still enforcing budgets when tracking was off, inconsistent with `RecordUsageAsync`); `ProviderRateLimiter` builds its limits dictionary last-wins (two case-variant entries for the same provider no longer throw `ArgumentException` at construction).
- **Verify-first (L007/L008)** — `AgentOptions` round-trip was already covered (CR-M007); added `LlmProviderFactoryTests` + `FromDictionary` tolerance tests. L008 (streaming-response leak) was already resolved by CR-M003 (`LlmStreamingResponse` is `IDisposable`/`IAsyncDisposable`, Agent enumerates under `await using`).
- Suites green: `Birko.AI.Tests` 20, `.Resilience.Tests` 13, `.Providers.Tests` 17, `.Agents.Tests` 18. STORY-027 now **13/418**.

### SqLiteConnector AUTOINCREMENT DDL fix — closes CR-M166 offline (TASK-058) (2026-07-14)
`SqLiteConnector.FieldDefinition` appended a bare `AUTOINCREMENT` after any other constraints for **any** `IsAutoincrement` field — invalid SQLite: the keyword is only legal as part of an `INTEGER PRIMARY KEY AUTOINCREMENT` constraint (adjacent to PRIMARY KEY, type exactly `INTEGER`), and never on a non-PK column. So a **dual-key** model (`[PrimaryField] Guid` + a separate non-PK `[IncrementField] Id`, e.g. `SqlSyncKnowledgeItem`) made `CreateTable` throw a syntax error. Fixed ([TASK-058](tasks/_loose/TASK-058-sqliteconnector-autoincrement-ddl-non-primary-key.md), option (a)): an autoincrement **PK** now emits `INTEGER PRIMARY KEY AUTOINCREMENT` as one adjacent clause; a non-PK `[IncrementField]` emits a **plain column** (SQLite has no per-column auto-increment outside the PK — the caller assigns the value; MSSql `IDENTITY` / PostgreSQL `SERIAL` differ, documented inline). **Verified offline (no Docker):** `Birko.Data.SQL.SqLite.Tests` 24 (no regression); the SQL sync-store CRUD round-trip now runs on a real SQLite `.db` (`Birko.Data.Sync.Sql.Tests` 2 → 6). This **closed CR-M166 offline** and removed it from the STORY-028 Docker pile → STORY-026 **267/275**, STORY-028 down to **8** findings.

### Code-review remediation — medium findings, bucket B (offline fix / infra-gated verify) (EPIC-014 / STORY-026) (2026-07-13)
Sixtieth STORY-026 batch: the "bucket B" items — a real offline fix, with full behavioral verification needing infra — **9 closed** (241→**266/275**). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Test-gaps closed offline** — **M143**: new **Birko.Data.SQL.PostgreSQL.View.Tests**; extracted the materialized-view DDL into internal `Build{Create,Refresh,Drop}MaterializedViewSql` helpers so the SQL text is assertable + the null/empty/non-view guards (sync + the async variants that validate before `Task.Run`). **M161**: ES sync `GenerateId`/`CreateKnowledgeItem` pure-logic (id format + IsLocalDeleted/IsRemoteDeleted derivation).
- **Code fixes with offline regressions** (mock / test-seam / loopback) — **M207**: `RedisConsumer` poll loop no longer dies silently; a new internal `PollError` event surfaces non-cancellation faults (in-loop → keep polling; terminating → `RemoveSubscription` + `RedisSubscription.IsActive` now reflects the registry). **M208**: the Redis consumer name gets a per-subscription suffix even when `ConsumerName` is configured (distinct PEL). **M209**: AutoAck poll-loop round-trip + poll-fault coverage over the mock-`IDatabase` seam. **M210**: `SmtpEmailSender` serializes sends with a `SemaphoreSlim` (SmtpClient isn't concurrency-safe); internal send-delegate seam proves 6 concurrent sends never overlap. **M193**: `SmtpHealthCheck` bounds the banner read with a linked-CTS timeout (a loopback half-open server now returns Unhealthy instead of hanging; caller cancellation still propagates). **M139**: MSSql indexed-view async methods (`CreateIndexedViewAsync`/`DropIndexedViewAsync`/`IndexedViewExistsAsync`) are now genuine async (DoCommandAsync/DoCommandWithTransactionAsync + ExecuteNonQueryAsync/ExecuteReaderAsync) instead of `Task.Run(sync)`. **M204**: `MqttConsumer` replays subscriptions on (re)connect via a new `ResubscribeAllAsync` hooked to the client's `ConnectedAsync` (CleanSession drops broker subs → previously silent message loss).
- **Deferred M138** (MSSql transactionless bulk-write atomicity) to the live-MSSql tier — the fix (wrap the six `SqlBulkCopy`/command bulk paths in a transaction) mutates write hot-paths and a mid-bulk failure can't be triggered offline. `InitException` semantics confirmed consistent with the framework-wide write contract.
- STORY-026 now **266/275**. The remaining **9** open all need Docker/a live server (ES/Cosmos/Raven/InfluxDB CRUD, live MSSql/Postgres, InfluxDB genuine-async, the Cosmos/Raven sync-store interface refactors M159/M164).

### Code-review remediation — medium findings, offline sweep round 2 (re-triage correction) (EPIC-014 / STORY-026) (2026-07-13)
Fifty-ninth STORY-026 batch: a re-triage found the earlier "everything left needs Docker" was wrong — many open findings had a real offline-doable component. Closed **13** across 5 clusters (241→257... i.e. **257/275**). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Health (M191/M192/M194)** — `AzureBlobHealthCheck`/`AzureKeyVaultHealthCheck` now rethrow `OperationCanceledException` ahead of the generic catch so `HealthCheckRunner`'s timeout-status logic applies (M191). The six HTTP checks (ElasticSearch/InfluxDb/RavenDb/Vault/CosmosDb/Sse) track `_ownsClient` and implement `IDisposable`, disposing only an internally-created `HttpClient` (M192). New **Birko.Health.Redis.Tests** (8, Moq-mocked `IConnectionMultiplexer`/`IDatabase`) covers RedisHealthCheck's Healthy/Degraded/Unhealthy branching + latency threshold + guards (M194).
- **Messaging.Razor (M211/M212)** — the three render methods observe the `CancellationToken` (before + around `CompileRenderStringAsync`) and exclude `OperationCanceledException` from the `TemplateRenderException` wrapping (M211); M212 verify-first (the .Tests project already covers traversal/caching/fallback/dispose), augmented with the cancellation regressions.
- **SQL.View (M146/M149)** — `SqLiteConnector.ViewExistsAsync` override queries `sqlite_master WHERE type='view'` (the async path had fallen through to the base probe that returns true for a same-named TABLE) (M146). Auto-mode `ShouldUsePersistentView` now caches only definitive positives, so a transient probe failure no longer poisons Auto mode and a view created after the first probe is picked up (M149). Tested against real SQLite in Birko.Data.SQL.Views.Tests.
- **MSSql (M136/M141)** — the RemoteSettings-but-not-MSSqlSettings branch of `CreateConnection` now derives an `MSSqlSettings` and delegates to `GetConnectionString()` (was a divergent inline string hard-coding MARS=False, no TrustServerCertificate/timeout) (M136). Birko.Data.SQL.MSSql.View.Tests gained the indexed-view API coverage — BuildCreateViewSql, the key-column ladder, guard exceptions (M141).
- **TimescaleDB (M176/M177/M178)** — the `RemoteSettings` ctor chains to the typed ctor so base `_settings` is a `TimescaleDBSettings` (CreateConnection no longer drops the TimescaleDB connection-string on that path) (M176); extracted `BuildCreateHypertableSql` (internal static) + broadened TimescaleDB.Tests to 14 (M177); new **Birko.Data.TimescaleDB.ViewModel.Tests** (5) for the repo ctor guard + SetSettings routing + lifecycle guards (M178).
- **Sync (M158)** — `ConvertToCosmosItem`'s pass-through branch now populates a null `Guid` (`??=`) in both the async + sync Cosmos knowledge stores, so the downstream `Guid!.Value` can't NRE.
- **Reclassified M159/M164** (Cosmos/Raven sync-store interface refactors) into the Docker Sync-backend cluster — they touch live-only stores and the interface-shape change can't be verified without running the sync provider. Remaining 18 open all need a live DB/broker or are bucket-B (offline fix, infra-gated verification).

### Code-review remediation — medium findings, view-SELECT refactor + Sync core test-gap (EPIC-014 / STORY-026) (2026-07-13)
Fifty-eighth STORY-026 batch: **CR-M140/M151** (view-SELECT-builder de-duplication) + **CR-M157** (Sync core test-gap) — the last three findings runnable without Docker. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **M140/M151 (cleanup)** — the ~140-line view-SELECT builder was copy-pasted in three places (base `AbstractConnectorBase.BuildViewSelectSql`, the MSSql SCHEMABINDING `BuildSchemaBindingSelectSql`, and the migration `ViewSqlGenerator`), and the copies would silently drift. Extracted a static **`ViewSelectSqlBuilder.BuildViewSelectSql(view, quoteIdentifier, qualifyTableName?)`** into Birko.Data.SQL.View, parameterized by a quote func + an optional FROM/JOIN table-qualifier (the only real inter-copy difference). The base delegates with plain `QuoteIdentifier`; `MSSqlConnector.BuildSchemaBindingSelectSql` calls it directly with a `[dbo].[Table]` qualifier (deleting its 3 duplicated methods); `ViewSqlGenerator` calls it with `id => QuoteIdentifier(id, quoteChar)` (deleting its 3 copies). **Deviated** from M140's "override QualifyTableName on the connector" — a connector-wide override would apply the schema prefix to *regular* (non-indexed) MSSql views too; the qualification is scoped to the SCHEMABINDING path only. Regressions: MSSql.View.Tests (6) assert two-part FROM/JOIN + identical aggregate-AS/GROUP-BY vs base + the two differ only by `[dbo].`; SQL.Tests ViewSqlGeneratorTests (+1) assert the generator output equals `"CREATE OR REPLACE VIEW <name> AS " + shared-builder-body`; Views.Tests (12) end-to-end SQLite join-view SQL unchanged.
- **M157 (test-gap)** — verify-first: bidirectional both-exist=Update, NewestWins-with-nullable-UpdatedAt (the CR-H098 regression), and cancellation were already covered. Closed the two genuine residuals: **delete propagation** (new `SyncProviderDeletePropagationTests`, 4 — remote/local-deleted tombstones delete the surviving copy on Download/Upload; a local-only item without a tombstone is a no-op; preview reports ToDelete without mutating) and **`AsyncSyncProvider`** (new `AsyncSyncProviderTests`, 8 — ctor guards + initial-download/update/upload/delete/preview/last-sync parity). Birko.Data.InMemory is sync-only, so added a minimal `InMemoryAsyncStore<T> : AbstractAsyncBulkStore<T>` + `TestAsyncBulkStore`/`TestAsyncSyncKnowledgeItemStore` doubles. Latent (per CR-M156): the core provider treats bidirectional both-exist as Update, not Conflict, so no conflict-resolution action test was written. Sync.Tests 29 → 41.
- STORY-026 now **244/275**. **Every finding runnable without Docker is closed;** the remaining 31 all need a live DB/broker (Cosmos/ES/Raven/InfluxDB, live MSSql/Postgres) — the deferred integration-test pile.

### Code-review remediation — medium findings, Sync file-backend test-gaps + cleanup (EPIC-014 / STORY-026) (2026-07-13)
Fifty-seventh STORY-026 batch: **CR-M162/M163/M171** closed (M166 partial). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **M162 (cleanup bug)** — `AsyncJsonSyncKnowledgeStore.SetLastSyncTimeAsync` re-serialized the whole JSON file once per item; now mutates all matching items then does one bulk `UpdateAsync(items)`. Same fix applied to the sibling `AsyncXmlSyncKnowledgeStore`.
- **M163** — new **`Birko.Data.Sync.Json.Tests`** (5, real temp-file store): last-sync-time round-trip (all items updated in one write), scope isolation, null/empty, CreateKnowledgeItem.
- **M171** — new **`Birko.Data.Sync.Xml.Tests`** (5, mirrors the JSON suite against a temp-file XML store).
- **M166 (partial)** — new **`Birko.Data.Sync.Sql.Tests`** covers `CreateKnowledgeItem`; the CRUD paths can't run on SQLite because the model's non-PK `[IncrementField] Id` (with a `[PrimaryField] Guid`) makes `SqLiteConnector` emit invalid `AUTOINCREMENT` DDL → moved to the Docker pile (needs real MSSql/Postgres). Surfaced a genuine SqLiteConnector DDL limitation for dual-key models. STORY-026 now **241/275**. Remaining runnable: M157 (Sync core — mostly covered) + M140/M151 (view-SELECT refactor).

### Code-review remediation — medium findings, view GroupBy + Tenant sync bugs (EPIC-014 / STORY-026) (2026-07-13)
Fifty-sixth STORY-026 batch: **CR-M153/M167/M168/M169/M170** — re-triaged the "un-runnable" pile and found most is offline/SQLite-verifiable; closed a view bug + the three Tenant sync bugs + a (stale) test-gap. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **M153 (bug)** — `SqlViewTranslator` never emitted GROUP BY metadata; the connector derives GROUP BY from the SELECTed non-aggregate fields, so a GroupBy field that wasn't also selected was silently dropped → wrong aggregates. `Translate` now rejects that case (the reverse is already caught by `ViewDefinitionBuilder.Build`). Views.Tests 10 → 12.
- **M167 (bug)** — `TenantSyncProvider.ExecutePreviewAsync` swallowed every failure as a spurious `Conflicts++` on a partial preview; now rethrows so real errors/cancellation surface.
- **M168 (bug)** — initial sync mutated the caller's `SyncOptions.Direction` (leaked via the returned-same-instance `TenantSyncOptions`) and reported the pre-override direction; now a local `effectiveDirection` drives Create routing + `result.Direction`.
- **M169 (bug, cross-tenant leak)** — `BelongsToTenant` returned `true` for an entity with a `TenantGuid` property but an unset/mismatched value → synced into/out of every tenant. Now returns `false`; allow-all is reserved for types with no `TenantGuid` property.
- **M170** — `Birko.Data.Sync.Tenant.Tests` already existed; augmented with `TenantSyncBugsTests` (M167/M168/M169 regressions), suite → 12. STORY-026 now **238/275**. The earlier "everything left needs Docker" was overstated — remaining runnable: M140/M151 (view-SELECT refactor) + offline test-gaps M157/M162/M163/M166/M171; Docker-only: M160/M161/M165 + live MSSql/Postgres.

### Code-review remediation — medium findings, migration async-interface refactor (EPIC-014 / STORY-026) (2026-07-13)
Fifty-fifth STORY-026 batch: **CR-M101** — threaded `CancellationToken cancellationToken = default` through the migration async surface. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Interfaces** — `IMigrationRunner` (InitializeAsync/MigrateAsync/RollbackAsync) + `IMigrationStore` (InitializeAsync/GetAppliedVersionsAsync/RecordMigrationAsync/RemoveMigrationAsync/GetCurrentVersionAsync).
- **`AbstractMigrationRunner`** threads it into the `Store.*Async(ct)` calls and the `ExecuteMigrationsAsync(…, ct)` hook.
- **6 backend stores** — `SqlMigrationStore` threads it into the real ADO.NET async calls + private helpers; the 5 SDK sync-wrapper stores (Cosmos/ES/Influx/Mongo/Raven) observe it via `ThrowIfCancellationRequested()` (genuine SDK-async = CR-M108, InfluxDB-gated). Defaulted param → source-compatible; all 6 backend `.Tests` build clean.
- **Regression** — `AsyncMigrationRunnerTests` 14 → 17 (cancelled-token throws for Initialize/Migrate/Rollback; the fake observes the token so this proves the runner *threads* it) + `SqliteMigrationRunnerTests` +2 (real-SQLite cancelled-path throws via `OpenAsync(ct)`). STORY-026 now **233/275**. This is the prerequisite for CR-M108/M109 (genuine-async InfluxDB + Flux count) — those still need a live InfluxDB, so they stay Docker-gated.

### Code-review remediation — medium findings, SQLite integration tier (EPIC-014 / STORY-026) (2026-07-13)
Fifty-third STORY-026 batch: **CR-M135/M144/M145/M150** — the start of the SQLite integration tier (Microsoft.Data.Sqlite, real on-disk `.db` files; the one integration tier verifiable here without Docker). Verify-first: `Birko.Data.SQL.SqLite.Tests` / `.ViewModel.Tests` / `.Views.Tests` already existed, so M135/M145 were "extend" not "create". [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Bulk retry bug (M144)** — the six `SqLiteConnector` native bulk methods (BulkInsert/Update/Delete + async) opened a connection+transaction and executed directly, **bypassing `ExecuteWithRetry`** — so SQLITE_BUSY/SQLITE_LOCKED (flagged transient by the connector's own `IsTransientException` override) failed immediately instead of retrying. Wrapped each body in `ExecuteWithRetry`/`ExecuteWithRetryAsync` (mirroring the base `RunCommandTransaction`; each attempt opens a fresh connection; cancellation still propagates).
- **Store CRUD + UnitOfWork (M135)** — new `SqLiteStoreCrudTests`: `AsyncSQLiteStore` single + bulk CRUD round-trips (drives the retry-wrapped bulk paths) and `SqlUnitOfWork` commit-persists / rollback-discards + state-machine guards. The isLock serialization + cancelled-token read were already covered by `AsyncConnectorLockAndCancellationTests`.
- **Settings + Guid + transient (M145)** — `SqLiteSettingsTests` (GetConnectionString/Path/LoadFrom) + `SqLiteTransientDetectionTests` (BUSY/LOCKED→transient, ERROR/CONSTRAINT→not, Timeout→transient); Guid→string conversion proven by the CRUD round-trip. `Birko.Data.SQL.SqLite.Tests`: 9 → 24.
- **ViewMigrationExtensions (M150)** — new **`Birko.Data.SQL.View.Migrations.Tests`** (11): CreateView/DropView (by-type + by-name, sync + async) assert the generated DDL + transaction propagation via a **recording fake `DbConnection`** (SQLite rejects `CREATE OR REPLACE VIEW`, so the fake asserts DDL/transaction wiring dialect-independently — the finding's "at minimum" approach), plus the non-`SqlMigrationContext` guard and null/empty guards. Registered in `.slnx` + `.code-workspace`.
- **Tier finish (M152/M154)** — augmented two existing green projects: `Birko.Data.SQL.ViewModel.Tests` (3 → 10, `SyncDataBaseRepositoryTests`: sync `DataBaseRepository` ctor guards, unwrapping `Connector`, `AddOnInit`/`RemoveOnInit` via `DoInit`, and `ReadOne` incl. a real SQLite round-trip) and `Birko.Data.SQL.Views.Tests` (3 → 10, `SqlViewTranslatorTests`: `Translate` name derivation / query-mode / COUNT(*) / null guard — the field/join mapping is already covered end-to-end by `SqlViewStoreAsyncTests`). STORY-026 now **232/275**; the SQLite tier is complete — every remaining open finding needs Docker/Testcontainers or the async-interface refactor.

### Code-review remediation — medium findings, TypeScript Birko.Web.* track (EPIC-014 / STORY-026) (2026-07-13)
Fifty-second STORY-026 batch: **CR-M256 … M266** — the whole TypeScript web-frontend track (11 closed: 8 bugs + 3 test-gaps) across `Birko.Web.Components` / `.Core` / `.Shell` (in the separate `Birko\Web` bucket; the audit paths are stale). No in-framework unit runner by design, so verified via the **Birko.Web.Playground** headless build + `backport-smoke` harness — **45/45** assertions (was 18), all 66 gallery components render, zero page errors; framework edits typecheck clean via Symbio.UI. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Listener/leak bugs** — `b-data-table` row-action `select` never unregistered the document `click` listener (M256, hoisted a shared `cleanup()`); `SyncManager.dispose()` leaked the `window 'online'` listener (M260, retained `_onlineHandler`).
- **Async-correctness bugs** — `b-command-palette` async search could commit a stale older response over a newer one (M257, `_searchSeq` token); `b-tree-menu` lazy `onExpand` rejection escaped as an unhandled rejection with no re-render (M258, `catch`→`load-error` + `update()` in `finally`).
- **State bugs** — `I18n` persisted the locale but never restored it on construct (M261, write-only → read `storageKey` back); `signal.ts` hardcoded the consumer `'symbio_'` persist prefix (M262, → neutral `'birko_'` default + `setPersistPrefix()`); `base-detail-page` (M264) and `base-form-modal` (M265) left the Save button spinning on an API rejection (try/finally, matching `base-crud-page`).
- **Test-gaps (M259/M263/M266)** — findings asked for a vitest runner (conflicts with the repo's deliberate no-in-framework-runner decision); per the user's call, extended the Playground backport-smoke harness with the highest-risk pure functions each names (`unwrapList`/`apiErrorMessage`/`I18n` plural; `renderMarkdown`/`b-pagination`; `getVisibleOptions`/`hasPermission`/`resolveModuleFromHash`). M258/M260/M261/M262 also each got a dedicated regression assertion. STORY-026 now **226/275** — the whole TypeScript track is closed; every remaining open finding is in the infra-heavy deferred pile (SQLite/Docker/async-refactor).

### Code-review remediation — medium findings, Workflow backend test-gaps (EPIC-014 / STORY-026) (2026-07-13)
Fifty-first STORY-026 batch: **CR-M268/269/270/271/272** — created the 5 missing Workflow-backend `.Tests` projects, closing **the entire offline .NET medium backlog**. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- New **`Birko.Workflow.JSON.Tests`** (5) / **`.MongoDB.Tests`** (3) / **`.RavenDB.Tests`** (3) / **`.CosmosDB.Tests`** (3) / **`.ElasticSearch.Tests`** (3), each covering the backend-independent `*WorkflowInstanceModel` round-trip: `FromInstance`→`ToInstance` (InstanceId/CurrentState/Status + Data & History survive the JSON serialization), `UpdateFromInstance` (CreatedAt preserved, UpdatedAt bumped, state/data/history updated), and Status enum casting. No live server needed — the store CRUD stays integration-tier. The backend SDK package refs are carried transitively by the `Birko.Data.X` projitems, so the test csprojs just mirror the corresponding `Birko.Data.X.Tests` import chain + `Birko.Workflow` + `Birko.Workflow.X`.
- All five git-initialized (local repos) and registered in `.slnx` + `.code-workspace`. STORY-026 now **215/275**; every remaining open finding is in the TypeScript `Birko.Web.*` track (M256–M266), verified via the Playground build rather than xUnit.

### Code-review remediation — medium findings, Security test-gaps AspNetCore + AzureKeyVault (EPIC-014 / STORY-026) (2026-07-13)
Fiftieth STORY-026 batch: **CR-M236/M237**. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **AspNetCore resolution path (M236, verify-first)** — the headline gap ("no tests for the per-request permission resolution path") is already covered by `PermissionResolutionMiddlewareTests` (5: authenticated populates both permission+role Items slots, no-tenant-claim → null, unauthenticated/invalid-userId no-op, default GetRolesAsync empty) and `ResolvedPermissionsCurrentUserTests` (8), plus the token→claims decode in TokenServiceAdapterTests — all added since the audit. Residual `?token=` query-string extraction + AddBirkoSecurity/AddBirkoJwtBearer DI registration left as minor integration-tier items.
- **AzureKeyVault write/token (M237)** — added `AzureKeyVaultWriteAndTokenTests` with a recording handler: SetSecretAsync success (PUT `/secrets/{key}`) + non-404 → HttpRequestException, DeleteSecretAsync success + the NotFound-is-OK branch, and the access-token cache/reuse (two GetSecret calls hit `login.microsoftonline.com` exactly once).
- Suites green: Birko.Security.AzureKeyVault.Tests 25. STORY-026 now **210/275**.

### Code-review remediation — medium findings, Vault.Configuration path/option logic (EPIC-014 / STORY-026) (2026-07-13)
Forty-ninth STORY-026 batch: **CR-M242** — verify-first. The Vault.Configuration projitems compiles into `Birko.Security.Vault.Tests`, whose `SecretConfigurationProviderTests` (added since the audit) already covers the `--`→`ConfigurationPath.KeyDelimiter` rewrite, recursive child traversal + prefix composition, multi-path ordering, throwing-ListSecrets graceful handling, and null guards. Added `LocalVaultConfigurationTests` for the remaining LocalVault pure logic — `BuildPaths` override-ordering (defaults → defaults.env → project → project.env → users/…, with domain scope and env/user omission) and `ResolveOptions` (User/Domain/Environment lower-casing + seeded-value-wins) via reflection on the private statics. No separate `.Tests` project needed. Suite green: Birko.Security.Vault.Tests 44. STORY-026 now **208/275**.

### Code-review remediation — medium findings, Structures CompressedTrie.Remove (EPIC-014 / STORY-026) (2026-07-13)
Forty-eighth STORY-026 batch: **CR-M251** — `Birko.Structures.Tests` already covered CompressedTrie GetWordsWithPrefix/GetAllWords (added since the audit); this batch closes the remaining `Remove` gap the finding flagged (existing-word with siblings preserved, missing/prefix-only word, remove-down-to-empty, and the merge-on-remove single-child case). All pass — the merge path is correct. Suite green: Structures trie tests 14. STORY-026 now **207/275**.

### Code-review remediation — medium findings, Workflow correctness (EPIC-014 / STORY-026) (2026-07-13)
Forty-seventh STORY-026 batch: **CR-M267/M273/M274/M275** — Workflow engine + backend correctness (4 closed; the M268–M272 backend test-gaps deferred as 5 new projects). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Half-applied state on fault (M267)** — `WorkflowEngine` advanced `CurrentState` to the target before running the target's OnEntry actions, but the history record is only appended after they succeed. A faulting OnEntry left the instance reporting `CurrentState = ToState` with a History missing that transition. The catch now restores `CurrentState = fromState`. Regression in Birko.Workflow.Tests.
- **Overload-shadowing footgun (M273)** — `RavenDBWorkflowInstanceStore`'s single-result reads used `ReadAsync(filter, ct)`, which the framework warns binds via the shadowed overload on a bulk store; switched the three to `ReadFirstAsync`.
- **SQL upsert race (M274)** — documented that `SqlWorkflowInstanceStore.SaveAsync` is a best-effort read-then-write and callers must serialize saves per InstanceId (concurrent double-create hits the Guid PK); a native MERGE is provider-specific. Also switched its single read to `ReadFirstAsync`.
- **XML history default (M275)** — `HistoryXml` defaulted to `"<ArrayOfTypeName />"`, which matches no real XmlSerializer root, so `ToInstance` threw on a non-overwritten value. Changed to `"<ArrayOfStateChangeRecord />"` and guarded `ToInstance` with `IsNullOrWhiteSpace`.
- Suite green: Birko.Workflow.Tests 32 (M267); M273/M275 code-review-verified (no `.Tests` sibling for those backends); M274 compile-verified via Workflow.SQL.Tests 7. STORY-026 now **206/275**.

### Code-review remediation — medium findings, Security test-gaps NFC + Vault (EPIC-014 / STORY-026) (2026-07-13)
Forty-sixth STORY-026 batch: **CR-M239/M241** — security test-gaps closed by augmenting existing `.Tests` projects. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **NFC token issuance (M239)** — `NfcAuthProvider.AuthenticateAsync`'s JWT branch (IssueTokens + ITokenProvider) was never exercised (the tests never passed a token provider). Added `NfcAuthProviderTokenTests` with a fake `ITokenProvider`: token issued on success with sub/nfc_uid/auth_method claims, email/name only when present on the mapping, and no token when IssueTokens=false or the provider is null.
- **Vault write success paths (M241)** — added `VaultSecretProviderWriteTests` with a request-capturing `HttpMessageHandler` asserting method/URI/body for `SetSecretAsync` (KV2 `{data:{...}}`→`/data/` vs KV1 flat), `DeleteSecretAsync` (KV2 `/metadata/` + NotFound-tolerated), and `GetSecretPairsAsync` KV2 inner-data → dictionary.
- Suites green: Birko.Security.NFC.Tests 46, Birko.Security.Vault.Tests 40. STORY-026 now **202/275**.

### Code-review remediation — medium findings, Storage.AzureBlob SAS/URL encoding (EPIC-014 / STORY-026) (2026-07-13)
Forty-fifth STORY-026 batch: **CR-M248/M249** in Birko.Storage.AzureBlob — **crosses 200/275**. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Raw blob path in URLs (M248)** — `GenerateSasUri` appended the raw `blobPath` into the SAS URI (and `AzureBlobStorage.GetBlobUri` into REST URLs); a key with spaces / `#` / `?` / `%` produced a malformed or mis-routed URL and could mismatch the SAS signature. Now percent-encodes each `/`-separated segment (slashes preserved) via a shared `EncodeBlobPath` helper, while the `canonicalResource` in the string-to-sign keeps the DECODED form per the Azure Service SAS spec (so the signature still matches). Regression asserts spaces/reserved chars encode and slashes survive.
- **Vacuous credential-guard tests (M249)** — `GetDownloadUrlAsync_WithoutAccountKey_ThrowsInvalidOperation` / `GetUploadUrlAsync_…` were non-async `[Fact]`s that called `act.Should().ThrowAsync<…>()` without awaiting — the assertion task was discarded, so they passed even if nothing threw. Made `async Task` + `await`.
- Suite green: Birko.Storage.AzureBlob.Tests 54. STORY-026 now **200/275**.

### Code-review remediation — medium findings, Structures/Storage/Telemetry/Time (EPIC-014 / STORY-026) (2026-07-13)
Forty-fourth STORY-026 batch: **CR-M246/247/250/252/253/254/255** — a cross-project reliability/cleanup sweep (7 closed). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Storage async ct (M246)** — `LocalFileStorage` Download/Delete/Exists/ListAsync accepted a `CancellationToken` but never observed it; added `ThrowIfCancellationRequested()` on entry (and inside the ListAsync `SearchOption.AllDirectories` tree-walk).
- **Storage path guard (M247)** — `ResolvePath`'s `StartsWith(_basePath)` let a sibling whose name extends the base pass; now requires exact equality or a trailing-separator boundary. Defensive (ValidateUserPath already blocks `..`).
- **DirectedGraph EdgeCount (M250)** — `Graph<T>.EdgeCount` was non-virtual and `DirectedGraph<T>` shadowed it with `new`, so a base `Graph<T>` reference got the undirected count/2. Made it `virtual`+`override`.
- **Telemetry ReadFirst (M252/M253)** — the bulk wrappers didn't override `ReadFirst`/`ReadFirstAsync`, so the IBulkStore default routed through the wrapper's single Read and bypassed the inner store's native single-row optimization; added delegating overrides + `InstrumentedBulkStoreWrapperTests`.
- **OpenTelemetry default (M254)** — `EnableAspNetCoreInstrumentation` defaulted true, forcing the OPTIONAL AspNetCore instrumentation package on every consumer (and meaningless-on for console/worker apps); flipped to opt-in `false`, consistent with the console-exporter toggles.
- **Time dead files (M255)** — deleted three orphaned duplicate provider/interface files (not in the Compile set, byte-identical to the imported Birko.Time.Abstractions), removing the drift / duplicate-type risk.
- Suites green: Structures 10, Storage 46, Telemetry 54, OpenTelemetry 11. STORY-026 now **198/275**.

### Code-review remediation — medium findings, Redis + Security hardening (EPIC-014 / STORY-026) (2026-07-13)
Forty-third STORY-026 batch: **CR-M231/232/233/234/235/238/240** across Birko.Redis + the Birko.Security.* family (7 closed). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Redis Lazy poisoned on first failure (M231)** — `RedisConnectionManager` built its `Lazy<ConnectionMultiplexer>` with the default `ExecutionAndPublication`, which caches the factory's exception, so a transient connect failure at startup permanently poisoned the singleton (every later access re-threw). Both ctors now use `LazyThreadSafetyMode.PublicationOnly` (does not cache exceptions → retried on next access). New **Birko.Redis.Tests** (M232, 10): GetConnectionString branch matrix, GetId, LoadFrom dispatch, manager null-guards / IsConnected-when-not-created / Dispose idempotency.
- **Pbkdf2 Verify not total (M233)** — `Pbkdf2PasswordHasher.Verify` guarded segment-count/algorithm/iterations but then called `Convert.FromBase64String` unconditionally, throwing FormatException on a corrupted/truncated stored hash. Wrapped in `try/catch (FormatException) → false`. Augmented **Birko.Security.Tests** (M234) with the Pbkdf2 malformed-input matrix + AES round-trip/GenerateKey/wrong-key/tamper.
- **Jwt null claims (M238)** — `JwtTokenProvider.GenerateToken` NRE'd inside the LINQ projection on null claims; added `ArgumentNullException.ThrowIfNull(claims)` matching the ctor's fail-fast style.
- **Vault KeyNotFound on malformed body (M240)** — `GetSecretPairsAsync` and `ParseKv2Response` used `GetProperty("data")`, throwing KeyNotFoundException on a 200 with an unexpected/empty body; both now use `TryGetProperty` and return null / empty (mirroring the NotFound handling).
- **AspNetCore missing using (M235)** — `HeaderTenantResolver` used `StringValues.FirstOrDefault` without `using System.Linq`, compiling only under the consumer's ImplicitUsings; added the explicit import.
- Suites green: Redis 10, Security 15, Jwt 10, Vault 35. STORY-026 now **191/275**.

### Code-review remediation — medium findings, Serialization ct-gates + Random (EPIC-014 / STORY-026) (2026-07-13)
Forty-second STORY-026 batch: **CR-M230/M243/M244/M245** — Serialization CancellationToken gates + stream test coverage, and a verified Random test-gap. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **XML async ignored the token (M243)** — `SystemXmlSerializer`'s SerializeAsync/SerializeAsync&lt;T&gt;/DeserializeAsync/DeserializeAsync&lt;T&gt; wrapped synchronous work in `Task.CompletedTask`/`Task.FromResult` and never checked the token. Added `cancellationToken.ThrowIfCancellationRequested()` to each (XmlSerializer has no async API, so the gate is the idiomatic fix).
- **Protobuf serialize ignored the token (M245)** — `ProtobufBinarySerializer`'s two `SerializeAsync` overloads returned `Task.CompletedTask` without consulting the token, while its `DeserializeAsync` already honored it via `Task.Run(..., ct)`. Added the gate so the two agree.
- **Serializer stream test-gap (M244)** — the eight stream ISerializer methods on each serializer were untested. Added stream round-trips (sync + async) for JSON (incl. the Utf8JsonWriter dispose-flush path), XML, and Protobuf, plus the async cancelled-token tests that lock in M243/M245.
- **Random Snowflake rollover (M230, verified)** — `Birko.Random.Tests` already covers the >4096-ID sequence-rollover path (`Next_ManyIdsAcrossSequenceRollover_StayValid`, 20 000 IDs) + `WaitNextMillisecond_ReturnsEpochRelativeTimestamp` (the CR-H133 root-cause); no work needed.
- Suites green: Birko.Serialization.Tests 81, Birko.Random.Tests (Snowflake) 8. STORY-026 now **184/275**.

### Code-review remediation — medium findings, MessageQueue.MQTT (EPIC-014 / STORY-026) (2026-07-13)
Forty-first STORY-026 batch: **CR-M203/M205** in Birko.MessageQueue.MQTT (2 closed; M204 deferred to a live broker). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Double message-handler attach (M203)** — `MqttConsumer.EnsureEventAttached` read-then-wrote a plain `_eventAttached` bool without synchronization, so two concurrent `SubscribeAsync` calls could both `+= OnMessageReceivedAsync`, dispatching every received message twice (and `Dispose` only detaches once). Now double-checked under a dedicated `_eventAttachLock`, so the handler attaches exactly once. Code-review verified (a concurrent TOCTOU a single-threaded test can't distinguish).
- **Test-gap (M205)** — `Birko.MessageQueue.MQTT.Tests` already exists (created since the audit). Augmented with `MqttTopicAndSettingsTests` covering the finding's named pure-logic surface: `MqttTopic.IsValidPublishTopic`/`IsValidSubscribeFilter`/`Matches` (wildcard semantics — `#`-must-be-last, `+`-stands-alone, level-count), `MqttProducer.ToMqttQos` (all three QoS levels), and `MqttSettings.GetId` (ClientId vs "auto") / `LoadFrom`. Suite green: 23. STORY-026 now **180/275**.

### Code-review remediation — medium findings, Models test-gaps (EPIC-014 / STORY-026) (2026-07-13)
Fortieth STORY-026 batch: **CR-M214/218/222/223/224/225** — the Models cluster test-gaps (6 closed). Verify-first: five projects already existed and covered each finding's flagged bug, so this augmented the clearest named gaps and created the one genuinely-missing project (SEO). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Already-covered, closed as verified:** M218 (Customers.Tests: Customer.CopyTo type/all-fields + CustomerAddress.LoadFrom — the exact CR-H127/H128 asks) and M225 (Models.SQL.Tests: ApplyToDatabase column-name+flags, metadata-only facets via GetPropertyMaps, GetTableNames).
- **Augmented existing projects:** M214 (Category.Tests → +GetSlugSource), M222 (Inventory.Tests → +InventoryDocument scalar round-trip asserting Lines stays empty per M221), M223 (Product.Tests → +ProductPropertiesLoadTests for the default-interface LoadProperties flatten/null-drop/null-dict normalization).
- **New project (M224):** **`Birko.Models.SEO.Tests`** (7) — model/VM LoadFrom round-trips incl. log fields, PropertyChanged → SEOObjectProperty aggregation, the SEO&lt;T&gt; guid filter, and SEOByPath exact/prefix/empty-path branches. Registered in `.slnx` + `.code-workspace`.
- Suites green: Category.Tests 9, Product.Tests 9, Inventory.Tests 3, SEO.Tests 7. STORY-026 now **178/275** — the entire Models cluster (M213–M229) is closed.

### Code-review remediation — medium findings, Models SQL mappings (EPIC-014 / STORY-026) (2026-07-13)
Thirty-ninth STORY-026 batch: **CR-M219/M220/M229** — SQL mapping convention/integrity gaps in Birko.Models.Customers.SQL + .Users.SQL (3 closed, 2 new test projects created with user approval). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Unbounded Customer columns (M219)** — CustomerMapping mapped only ToTable + Guid, leaving Name/Code/Email/Phone/Website/TaxId/VatId as unbounded (nvarchar(max)-style, unindexable) columns. Added `HasPrecision` to each (256/64/256/64/256/32/32), matching ContactPersonMapping / Pricing.SQL. Left `IsUnique()` off TaxId/VatId/Code (the finding's "consider" — nullable natural keys, a DB/migration decision).
- **Unbounded Address family (M220)** — AddressMapping + InvoiceAddressMapping likewise mapped no string columns. Bounded the whole address shape (Name/Street 256, ZIP 16, Country 128, …); InvoiceAddress : Address re-maps the inherited address columns (each mapping owns its table) plus BIN/TIN/VATIN/BankAccount.
- **UserLogin natural key (M229)** — (Provider, ProviderKey) had no constraint. Recorded it as a shared composite index (`UX_UserLogin_Provider_ProviderKey`, order 0/1) and documented that mapping index/unique metadata is advisory (not emitted by `ApplyToDatabase`) and there's no composite-UNIQUE mapping primitive, so the UNIQUE constraint is enforced via migration — the mapping records intent/natural key.
- New **Birko.Models.Customers.SQL.Tests** (22) + **Birko.Models.Users.SQL.Tests** (3) assert the precision/index facets via `ModelMapRegistry.GetPropertyMaps`; registered in `.slnx` + `.code-workspace`. STORY-026 now **172/275**.

### Code-review remediation — medium findings, Models cluster (EPIC-014 / STORY-026) (2026-07-13)
Thirty-eighth STORY-026 batch: **CR-M213/215/216/217/221/226/227/228** across the Birko.Models.* domain projects (8 closed). Most Models `.Tests` siblings already existed; created one new (`Birko.Models.Users.Tests`) with the user's approval. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **ValueData dropped log fields (M213)** — `ValueData.LoadFrom(ViewModels.Value)` set only Price/PriceVAT/VAT and never called `base.LoadFrom`, silently discarding CreatedAt/UpdatedAt/PrevUpdatedAt + identity (unlike the sibling AbstractPercentage/AbstractTree). Added `base.LoadFrom(data)` (ViewModels.Value : LogViewModel : ILogEntity).
- **HierarchyHelper NRE (M215)** — `RewriteDescendantPaths` dereferenced `desc.Path` unguarded, NRE-ing mid-batch on a freshly-constructed entity whose Path was never materialized (partial mutation). Now null-guards oldPath/newPath at entry and skips any descendant whose Path is null or doesn't start with oldPath; the NamePath branch guards a null NamePath too. (M216 Contracts test-gap: the project already exists — augmented with these cases.)
- **Users FK crash (M226)** — every relational model's FK overload did `XGuid = data.Guid!.Value`, throwing `InvalidOperationException` for a transient (unsaved, Guid == null) view model — the `!` silenced the warning but became a runtime crash. Replaced with `if (data?.Guid is Guid g) X = g;` across UserLogin/UserProfile/UserRole/UserTenant/RolePermission (7 overloads). New **Birko.Models.Users.Tests** (registered in .slnx + .code-workspace) covers the guard + `GetDisplayName`.
- **Documented partial-projection intents (M217/M221/M227)** — the Customer VM (only Name/Code/PriceGroup), InventoryDocument VM (no Lines), and the relational Users VMs (no parent FKs) are intentionally partial: documented on each `LoadFrom` that the omitted fields are set via other paths (store/domain services / relation-specific overloads) and a single VM→model load is not a full round-trip, rather than widening the view models (a breaking design change).
- Suites green: Birko.Models.Tests 35, Birko.Models.Contracts.Tests 7, Birko.Models.Users.Tests 7. STORY-026 now **169/275**. (Remaining Models test-gaps M214/218/222/223/225 have existing projects to augment; M219/220/224/229 need new .Tests projects.)

### Code-review remediation — medium findings, MessageQueue.Redis token gate (EPIC-014 / STORY-026) (2026-07-13)
Thirty-seventh STORY-026 batch: **CR-M206** in Birko.MessageQueue.Redis — offline. `RedisProducer.SendAsync` and `RedisConsumer.AcknowledgeAsync`/`RejectAsync` accepted a `CancellationToken` but never observed it; added `cancellationToken.ThrowIfCancellationRequested()` on entry (StackExchange.Redis has no per-call token, so the entry gate mirrors the framework's `EnsureInitializedAsync` pattern; the generic `SendAsync<T>` delegates to the gated non-generic). Offline regression asserts a pre-cancelled token throws before any Redis call (lazy connection). Suite green: Birko.MessageQueue.Redis.Tests 41. STORY-026 now **161/275**. (MQTT M203/M205 need a new `.MQTT.Tests` project; M204/M207/M208/M209 need a live broker — deferred.)

### Code-review remediation — medium findings, MessageQueue core + InMemory (EPIC-014 / STORY-026) (2026-07-13)
Thirty-sixth STORY-026 batch: **CR-M199/M200/M201/M202** (Birko.MessageQueue core + .InMemory) — offline. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **RetryPolicy overflow + tests (M199)** — the core `RetryPolicy.GetDelay` had no tests; adding the large-attempt boundary case surfaced the **same overflow fixed in Birko.Contracts under CR-M078**: `(long)Math.Pow(2, attemptNumber - 1)` wrapped to a negative value for large attempts, producing a negative TimeSpan that slipped past the `> MaxDelay` clamp. Now computes in `double` and saturates at `MaxDelay` before the cast. New `RetryPolicyTests` covers fixed/exponential/clamp/factory defaults + the 53/100/1000/int.MaxValue boundary.
- **InMemory CTS dispose (M200)** — already resolved under CR-H119 (verify-first): `RemoveSubscriber` captures, nulls, then Cancel()+Dispose()s the dispatch CTS, and `StartDispatching` only runs when the field is null. Covered by the existing `InMemoryChannelDisposalTests`.
- **Delayed send fire-and-forget (M201)** — `InMemoryProducer.SendAsync`'s delayed branch spawned an untracked `Task.Run` that swallowed post-delay faults. Now attaches an `OnlyOnFaulted` continuation to observe the fault (no unobserved-task exception) and documents delayed delivery as best-effort (a failure after the delay can't be reported through the already-completed call).
- **RejectAsync requeue lost the message (M202)** — `InMemoryConsumer.RejectAsync(requeue: true)` had an empty if-body because it couldn't recover the destination, so requeue behaved identically to discard. `_pendingAck` now stores `(destination, message)`, so requeue writes the message back to its channel via `_channel.WriteAsync`.
- Regression tests (offline): RetryPolicy boundary/growth/clamp; InMemory requeue-redelivers vs no-requeue; delayed-send delivers and a cancelled delayed send neither throws nor delivers. Suites green: Birko.MessageQueue.Tests 71, Birko.MessageQueue.InMemory.Tests 11. STORY-026 now **160/275**. (MQTT M203/M205 need a new .MQTT.Tests project; M204/M207/M208/M209 need a live broker — deferred.)

### Code-review remediation — medium findings, Helpers + Localization (EPIC-014 / STORY-026) (2026-07-13)
Thirty-fifth STORY-026 batch: **CR-M195/M196** (Birko.Helpers) + **CR-M197/M198** (Birko.Localization / .Localization.Data) — offline. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **SanitizePath single-pass (M195)** — `PathValidator.SanitizePath` ran the four traversal-token `Replace`s once each, so overlapping/nested sequences re-formed a token after one pass (`....//` → `../`, `..././` → `../`). Now loops the replacements in a `do/while (path != previous)` until stable. Regression (`SanitizePathTests`) asserts no `../`/`..\` token survives a battery of nested inputs.
- **Helpers test-gap (M196)** — added `CsvParserAndPathHelperTests` to Birko.Helpers.Tests for the two highest-risk untested helpers: `CsvParser` (simple/quoted-with-delimiter+newline/doubled-quote-escape/CRLF/trailing-row/custom-delimiter) and `PathHelper.IsUnderDirectory` (containment, identical, trailing separator, and the sibling-prefix that must not match). Other helper types deferred to a later coverage sweep.
- **InMemory culture guard (M197)** — `InMemoryTranslationProvider.GetSupportedCultures` called `CultureInfo.GetCultureInfo` unguarded, so a translation added under a bogus culture name threw `CultureNotFoundException`. Now `try/catch → null` + `.Where(c => c != null)`, matching the Json/Resx providers.
- **Sync-over-async doc (M198)** — documented on `DatabaseTranslationProvider`'s three sync members that they block on async store I/O and can deadlock on a single-threaded sync context; steer callers to the `*Async` overloads.
- Suites green: Birko.Helpers.Tests 88, Birko.Localization.Tests 140. STORY-026 now **156/275**.

### Code-review remediation — medium findings, Birko.EventBus cluster (EPIC-014 / STORY-026) (2026-07-13)
Thirty-fourth STORY-026 batch: **CR-M184 … M190** — the whole EventBus cluster (7 findings across Birko.EventBus, .EventSourcing, .MessageQueue, .Outbox), all offline-verified in the single `Birko.EventBus.Tests` project (compiles every EventBus projitems, so no new test projects). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Parallel Stop didn't stop (M184)** — `InProcessEventBus.DispatchParallelAsync` eagerly launched every handler, so `ErrorHandlingMode.Stop` never halted the rest on first failure. Now runs handlers under a linked `CancellationTokenSource` cancelled on the first failure — queued handlers (waiting on the semaphore) and token-observing ones abort — and the original handler exception (not a follow-on cancellation) is captured and rethrown via `ExceptionDispatchInfo`.
- **Silent handler failures (M185)** — the Continue/Stop catch blocks were empty and there was no logger, so a throwing handler vanished. Added a dependency-free `Action<IEvent, Exception>? OnHandlerError` to `InProcessEventBusOptions`, invoked in both modes across sequential and parallel dispatch; the doc now states failures are dropped unless it is set.
- **Replay lost event time/identity (M186)** — `DomainEventPublished` copied everything from the source domain event except `OccurredAt`/`EventId`, so `EventBase` stamped the wrapper with the current time and a fresh Guid — defeating time-ordered and dedup-by-EventId replay. The ctor now propagates both.
- **Unused retry/DLQ options (M187, design)** — documented that `DistributedEventBusOptions.RetryPolicy`/`DeadLetterOptions` are delegated to the underlying transport (the CR-H114 dispatch faults the delivery callback and the provider re-delivers/DLQs) rather than consumed by the bus; chose this over wiring bus-level retry (conflicts with the transport-driven model) or removing the properties (breaking).
- **Sync-over-async dispose (M188)** — `DistributedEventBus.Dispose` blocked on `UnsubscribeAsync().GetAwaiter().GetResult()`. Now implements `IAsyncDisposable` (awaits `UnsubscribeAsync`), and synchronous `Dispose` calls `sub.Dispose()` (ISubscription : IDisposable).
- **Opaque outbox error (M189)** — `OutboxProcessor.PublishEventAsync` used `MethodInfo.Invoke`; a synchronous throw surfaced as `TargetInvocationException` ("Exception has been thrown by the target of an invocation."), which `MarkFailedAsync` recorded into `LastError`. Now unwraps at the Invoke site (`Task.FromException(inner)`) plus an `Unwrap` helper in the ProcessBatch catch, so `LastError` carries the real cause.
- **Outbox test-gap (M190)** — already covered by the existing `Outbox/` tests in `Birko.EventBus.Tests`. Fixed a **pre-existing compile break** while here: `InMemoryOutboxStoreTests` still called the old 2-arg `MarkFailedAsync` after the CR-H115 `maxAttempts` parameter landed, so the whole test project failed to build — updated to `maxAttempts: 5`.
- Suite green: Birko.EventBus.Tests 84. STORY-026 now **152/275**.

### Code-review remediation — medium findings, Birko.Data.XML store (EPIC-014 / STORY-026) (2026-07-13)
Thirty-third STORY-026 batch: **CR-M182 / M183** in Birko.Data.XML (2 offline file-based bugs). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Empty-store disk re-read (M182)** — the async `EnsureDataLoadedAsync` gated loading on `_items == null || _items.Count == 0`, so a store that genuinely persists zero rows (empty file, or after all rows deleted) re-opened and re-deserialized the file on every single read/count/aggregate call. Added a `protected bool _loaded` on `AbstractAsyncXmlStore`; the gate is now `!_loaded` (set true after `LoadDataAsync`), and `DestroyAsync` (AsyncXmlStore + AsyncXmlSeparateStore) resets it so a later re-init reloads. The sync `XmlStore` loads in its constructor so has no per-call re-read — M182 is async-only.
- **Save data-loss window (M183)** — `XmlStore.SaveData` and `AsyncXmlStore.SaveDataAsync` did `File.Delete(Path)` then `OpenWrite`/`FileMode.Create`, so a serialization failure between the delete and a complete write (non-serializable property, cancellation, process kill) left the data file gone or partial — total data loss. Both now write to `Path + ".tmp"` then `File.Move(tmp, Path, overwrite: true)`, with a catch that deletes the temp and rethrows; the previous file stays intact until the new one is fully written. The redundant pre-`FileMode.Create` delete is gone.
- Regression tests (Birko.Data.XML.Tests): an empty store loads from disk exactly once across multiple reads and reloads once after destroy (M182); a serializer that throws on the second write leaves the first file byte-identical with no leftover `.tmp` (M183). Suite green: 16. STORY-026 now **145/275**.

### Code-review remediation — medium findings, Birko.Data.Tagging + Birko.Data.Tenant (EPIC-014 / STORY-026) (2026-07-13)
Thirty-second STORY-026 batch: **CR-M172** (Tagging N+1 cleanup) + **CR-M175** (Tenant design doc). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **SetEntityTags N+1 (M172)** — `TagService.SetEntityTagsAsync` computed the add-set against the already-loaded current links, but the add loop called `AttachTagAsync`, which re-issues `GetEntityTagLinksAsync` + a duplicate re-scan on every iteration — N extra link queries for N added tags, all redundant since the `!currentTagIds.Contains(id)` filter already guarantees the link is absent. The loop now calls `CreateEntityTagAsync(new EntityTag{...})` directly (the same payload `AttachTagAsync` builds). Regression asserts adding 3 tags issues exactly 1 link query via a call-counter on the in-memory service.
- **Static Tenant.Current footgun (M175, design/"other")** — the process-wide static `Tenant.Current` coexists with the DI-scoped `ITenantContext`; a store/repo accidentally constructed without a context falls back to the static instance (whose tenant the request middleware never set), silently degrading to unfiltered cross-tenant access. Documented the footgun on the `Tenant` class + `Current` property: non-DI scenarios (console/tools/tests) only; in a DI app always resolve a scoped `ITenantContext`. Chose docs over making the fallback throw (which would break legitimate non-DI construction) — the finding's primary recommendation.
- Suite green: Birko.Data.Tagging.Tests 11. STORY-026 now **143/275**.

### Code-review remediation — medium findings, Birko.Data.ViewModel bulk repos (EPIC-014 / STORY-026) (2026-07-13)
Thirty-first STORY-026 batch: **CR-M179 / M180 / M181** in Birko.Data.ViewModel (2 clean offline bugs + a test-gap that was already resolved). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Async bulk bypassed change-tracking (M179)** — `AbstractAsyncBulkViewModelRepository` inlined `CreateModelInstance()+MapToModel` / `CreateInstance()+LoadFrom` instead of the base `LoadModelInstance`/`LoadInstance` helpers, so bulk `ReadAsync` never called `StoreHash`. The change-tracking dictionary stayed empty for bulk-read entities, and a subsequent single-item `UpdateAsync` then saw no stored hash — `CheckHashChange` always returned true, defeating the optimistic no-op-update skip. `ReadAsync` now routes through `LoadInstance(model)` and CreateAsync/UpdateAsync/DeleteAsync(IEnumerable) through `LoadModelInstance(vm)`, matching the sync bulk repo (which was already correct).
- **ReadMode guard missing on bulk writes (M180)** — the single-item Create/Update/Delete throw `AccessViolationException("Repository is in Read Mode")` when `ReadMode` is set, but none of the twelve bulk mutators (sync + async: Create / Update×3 / Delete×2) did, so a read-only repository could still mutate the store via the bulk API. Added the same guard (same exception type/message) to all twelve.
- **Test-gap (M181)** — `Birko.Data.ViewModel.Tests` already exists (created since the audit; `BulkViewModelRepositoryDelegateTests` covers the CR-H110 delegate-transform contract). Augmented with `BulkViewModelRepositoryReadModeTests`: M180 ReadMode enforcement across all bulk ops + M179 async-bulk StoreHash priming (a `StoreHash`-tracking subclass proves `ReadAsync` hashes each entity). Also dropped two redundant projitems imports (Contracts/Configuration arrive transitively) to clear the MSB4011 warnings.
- Suite green: Birko.Data.ViewModel.Tests 9. STORY-026 now **141/275**.

### Code-review remediation — medium findings, Birko.Data.Tenant bulk + Save (EPIC-014 / STORY-026) (2026-07-13)
Thirtieth STORY-026 batch: **CR-M173 / M174** in Birko.Data.Tenant (2 clean offline bugs). The Sync-backend cluster (M157–M171) was skipped this pass as infra/new-project-dense (async-provider test double, Cosmos/ES/Raven/Sql live stores, Sync.Json/Xml new file-based test projects) — folded into the deferred pile; core offline projects are being swept instead. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Bulk double-enumeration (M173)** — `TenantBulkStoreWrapper` and `AsyncTenantBulkStoreWrapper` Delete/Update validated `data.All(BelongsToCurrentTenant)` then passed the same `data` to the inner store, enumerating a lazy source twice; a non-deterministic sequence could persist a set different from the one authorized (defeating the tenant check). All four now materialize once (`data as IReadOnlyCollection<T> ?? data.ToList()`).
- **SaveAsync lost id (M174)** — `AsyncTenantStoreWrapper.SaveAsync`'s create branch discarded the `CreateAsync` return and returned `data.Guid ?? Guid.Empty`, relying on the inner store mutating `data.Guid` in place (not guaranteed by `IAsyncStore`). Now `return await CreateAsync(...)`, matching the sync `TenantStoreWrapper.Save`.
- Offline tests (Birko.Data.Tenant.Tests): counting-enumerable single-enumeration through DeleteAsync/UpdateAsync; SaveAsync returns a fake inner store's internally-allocated id. Suite green: Tenant.Tests 16. STORY-026 now **138/275**.

### Code-review remediation — medium findings, Birko.Data.Sync Preview + bidirectional (EPIC-014 / STORY-026) (2026-07-09)
Twenty-ninth STORY-026 batch: **CR-M155 / M156** in Birko.Data.Sync (2 clean offline bugs). The remaining SQL-cluster findings (M136, M138–M146, M149–M154) are deferred as infra-heavy (new test projects, live-DB fixes, view-builder refactors, and the M153 GROUP-BY-metadata change) after clearing the clean offline SQL bugs (M137/M142/M147/M148). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Preview swallowed cancellation (M155)** — `SyncProvider.Preview` / `AsyncSyncProvider.PreviewAsync` wrapped the whole body in a bare `catch { preview.Conflicts++; return preview; }`, converting any failure — including `OperationCanceledException` from an async `ReadAsync(token)` — into a normal-looking preview with the conflict counter bumped, so callers couldn't tell "one real conflict" from "the whole preview blew up" and cancellation was masked. Added `catch (OperationCanceledException) { throw; }` ahead of the broad catch in both. (A dedicated failure flag on `SyncPreview` for the other-exception case is a larger model change, noted as a limitation.)
- **Dead bidirectional conflict branch (M156)** — `DetermineSyncAction`'s both-exist branch checked `if (winner == "conflict")`, but `GetWinner` only ever returns "local"/"remote" (via `GetNewest`), never "conflict" — so the `ConflictInfo` construction was unreachable. Removed the dead block (behavior-preserving — it always fell through to `Update` anyway); genuine both-diverged detection (compare local/remote versions to the last-synced knowledge version) is a tracked feature, flagged inline.
- Offline tests (Birko.Data.Sync.Tests): a store whose bulk read throws OCE makes Preview rethrow (not mask); a bidirectional both-exist item previews as Update with zero Conflicts. Suite green: Sync.Tests 29. STORY-026 now **136/275**.

### Code-review remediation — medium findings, Birko.Data.SQL.View ctor + GetViewField (EPIC-014 / STORY-026) (2026-07-09)
Twenty-eighth STORY-026 batch: **CR-M147 / M148** in Birko.Data.SQL.View (2 clean offline logic bugs). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **View ctor discarded the explicit name (M147)** — the guard was inverted: `if (!string.IsNullOrEmpty(name) && Tables.Any())` overwrote `Name` with the concatenated table names whenever a name **was** supplied, so `new View(tables, joins, "OrderSummary")` silently became "OrdersCustomers". Inverted to `string.IsNullOrEmpty(name)` — derive from tables only when none was given.
- **GetViewField null!/cast (M148)** — `(UnaryExpression)expr.Body` threw `InvalidCastException` for a plain `MemberExpression` body (`x => x.Name` on a reference type, no boxing), and the method returned `null!` (an opaque NRE at the immediate-deref callers) when no `[ViewField]` mapping was found. Now extracts the property from either body shape and throws a descriptive `ArgumentException`/`InvalidOperationException`.
- Both are in Birko.Data.SQL.View but tested in the existing **Birko.Data.SQL.Tests** (which compiles the `Birko.Data.SQL.View` projitems), so no new test project. Suite green: SQL.Tests 284. STORY-026 now **134/275**.

### Code-review remediation — medium findings, SQL providers MSSql + PostgreSQL type mapping (EPIC-014 / STORY-026) (2026-07-09)
Twenty-seventh STORY-026 batch: **CR-M137 / M142** (2 clean offline type-mapping bugs). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **MSSql blob truncation (M137)** — `MSSqlConnector.ConvertType` mapped `DbType.Object`/`Binary` to bare `BINARY`, which SQL Server defaults to `BINARY(1)` — every `byte[]`/serialized column created 1 byte wide, silently truncating blobs. Now `VARBINARY(MAX)`. Offline test in the (since-the-audit) Birko.Data.SQL.MSSql.Tests.
- **PostgreSQL brittle SERIAL (M142)** — `PostgreSQLConnector.FieldDefinition` injected SERIAL via `String.Replace` on the already-composed field definition (`sqlType.Replace("INTEGER","SERIAL")` etc.) — brittle whole-string surgery that could mutate any fragment containing the token. Now emits the `SERIAL`/`BIGSERIAL`/`SMALLSERIAL` pseudo-type directly at type-emit time when `IsAutoincrement`. Offline tests (autoincrement → SERIAL, plain → INTEGER).
- Both provider test projects already existed (created since the audit) so no new projects. Suites green: SQL.MSSql.Tests 17, SQL.PostgreSQL.Tests 14. STORY-026 now **132/275**. The remaining SQL-cluster findings (M136, M138–M141 MSSql/.View, M143–M155: live-DB retry/async, the base view-select-builder refactor, SQL.View ctor/nullable bugs, and the new provider-.View / SQL.View test projects) continue next.

### Code-review remediation — medium findings, Birko.Data.SQL connector (EPIC-014 / STORY-026) (2026-07-09)
Twenty-sixth STORY-026 batch: **CR-M134** closed, **CR-M135** deferred. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **InitException fall-through (M134)** — the connector reader paths caught a `createCommand`/`OnExecute` failure, called `InitException` (which **returns** instead of rethrowing when an `OnException` handler is registered), then unconditionally ran `ExecuteReader` on the malformed/empty command — a second, more confusing error (or silently wrong results) for exactly the callers who set a handler expecting it to terminate the op. All three reader paths (`RunReaderCommand`, the external-transaction reader variant, and the async `RunReaderCommandAsync`) now track a `faulted` flag and `yield break` before `ExecuteReader`/`ExecuteReaderAsync`. Code-review verified — Birko.Data.SQL.Tests fakes only `DbCommand` (TestDbCommand/TestDbParameter), not a `DbConnection`, so the reader path can't be driven end-to-end offline.
- **Deferred (M135)** — store CRUD (DataBaseStore/AsyncDataBaseStore/*BulkStore), RunCommand(Async), SqlUnitOfWork, and the isLock/CancellationToken behavior all run against a real `DbConnection`; with no fake-connection harness or wired SQLite provider in SQL.Tests, this needs a System.Data.SQLite integration tier — out of scope for the pure-logic pass, left open rather than falsely closed.
- STORY-026 now **130/275**.

### Code-review remediation — medium findings, RavenDB.ViewModel + Repositories (EPIC-014 / STORY-026) (2026-07-09)
Twenty-fifth STORY-026 batch: **CR-M132 / M133** (2 closed) — both test-gaps. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **RavenDB.ViewModel test-gap (M132)** — new **`Birko.Data.RavenDB.ViewModel.Tests`**: constructor store-type validation (plain `AsyncRavenDBStore` / wrapper accepted; foreign store → `ArgumentException`) + the unwrapping `RavenDBStore` getter (fake `IAsyncStore`+`IStoreWrapper` unwraps via the property where the direct cast is null). Offline; registered in `.slnx` + `.code-workspace`. Fourth in the ViewModel-repo family (ES/InfluxDB/Mongo/RavenDB), all sharing the ctor-guard + unwrap-getter pattern.
- **Repositories test-gap (M133)** — Birko.Data.Repositories.Tests existed (AsyncBulkRepositoryDestroyTests CR-H080 + RepositoryLocatorConcurrencyTests); augmented with `AbstractRepositoryFallbackTests`: the null-store fallbacks (`Read`→null, `Create`/`Save`→`Guid.Empty`, `Count`→0, `CreateInstance`→`Activator.CreateInstance<T>()`, Update/Delete/Destroy no-op) and the `AbstractBulkRepository` type-mismatch guard (Read/Create without an `IBulkStore` → `InvalidOperationException`).
- Suites green: RavenDB.ViewModel.Tests 4, Repositories.Tests 10. STORY-026 now **129/275**.

### Code-review remediation — medium findings, Birko.Data.RavenDB (EPIC-014 / STORY-026) (2026-07-09)
Twenty-fourth STORY-026 batch: **CR-M130 / M131** in Birko.Data.RavenDB (2 closed). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Bulk-insert cancellation (M130)** — `AsyncRavenDBStore.CreateCoreAsync(IEnumerable<T>)` accepted a `ct` but opened `_documentStore.BulkInsert()` with no token and called `StoreAsync(item)` untokenized, so a long bulk insert couldn't be cancelled. Now `BulkInsert(token: ct)` + a per-item `ct.ThrowIfCancellationRequested()`. Code-review verified (live-Raven path).
- **Test-gap (M131)** — Birko.Data.RavenDB.Tests (RavenDBIndexManagerTests + RavenDBStoreLazyInitTests, since the audit) augmented with `RavenDBSettingsAndUnitOfWorkTests`: Settings `GetId` / `LoadFrom` round-trip + foreign-type fallback / `CreateDocumentStore` config, and the `RavenDbUnitOfWork` state machine (Begin/double-Begin/Commit-Rollback-guards/post-Dispose, ctor null-guard) against an offline (lazy, uninitialized-connection) `DocumentStore`. Store CRUD, the facet-vs-LINQ aggregation execution, and Commit (SaveChanges) stay integration-tier; `MapFacetResults` is offline-testable in principle but needs FacetResult/AggregateQuery fixtures (deferred).
- Suite green: Birko.Data.RavenDB.Tests 30. STORY-026 now **127/275**.

### Code-review remediation — medium findings, Birko.Data.Processors (EPIC-014 / STORY-026) (2026-07-09)
Twenty-third STORY-026 batch: **CR-M127 / M128 / M129** in Birko.Data.Processors (3 closed). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **ZIP nested entry (M127)** — already resolved: the CR-H076 Zip Slip hardening flattens `entry.FullName` to `Path.GetFileName(...)`, so a nested `export/data.csv` extracts as `data.csv` into the (pre-created) `_extractPath` — no missing subdirectory / `DirectoryNotFoundException`. The finding's premise (targeting `_extractPath/export/data.csv`) is stale. Added a regression test that a folder-nested entry extracts + processes without throwing.
- **CSV fake-async (M128)** — `CsvProcessor.ProcessStreamAsync` runs the synchronous `CsvParser.Parse()` (blocking `reader.Read()`) and awaits only the per-row callbacks + cancellation checks. Documented via a `<remarks>` (the finding's accepted "at minimum" fix) that it's a convenience wrapper for async callers, not genuinely non-blocking; a real async parse would be a separate IAsyncEnumerable rewrite.
- **Xml async Value (M129)** — `XmlProcessor.ProcessStreamAsync` sets `XmlReaderSettings.Async = true` but read Text/CDATA via the synchronous `reader.Value` getter, which can throw `InvalidOperationException` on large/unbuffered values. New `ProcessNodeAsync` reads them via `await reader.GetValueAsync()`; Element/EndElement (which only touch `reader.Name`) delegate to the unchanged sync `ProcessNode`. The sync `ProcessStream` path is untouched.
- Suite green: Birko.Data.Processors.Tests 36. STORY-026 now **125/275**.

### Code-review remediation — medium findings, Birko.Data.Patterns (EPIC-014 / STORY-026) (2026-07-09)
Twenty-second STORY-026 batch: **CR-M124 / M125 / M126** in Birko.Data.Patterns (3 closed). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Bulk-wrapper double-enumeration (M124)** — the Sluggable bulk wrappers resolved slugs in a `foreach` over `data` and then passed the **same** `data` to the inner store, enumerating a lazy/one-shot source twice (the second pass persists unmutated objects). Fixed `AsyncSluggableBulkStoreWrapper.CreateAsync` and the sync `SluggableBulkStoreWrapper.Create`/`Update` to materialize once (`data as IReadOnlyList<T> ?? data.ToList()`); the async `UpdateAsync` was already fixed under CR-H075, and the SoftDelete/Audit/Timestamp bulk wrappers use a lazy `Select` projection applied on the inner store's single enumeration (safe) so were left as-is.
- **Versioned wrapper silent lost-update (M125)** — `UpdateAsync`/`Update` only ran the version check `if (existing != null …)`, so a null read (row not found / not yet visible) skipped the check and wrote with `data.Version++` — a lost-update. A missing row is now a conflict (`ConcurrentUpdateException`), and both wrappers carry a doc comment that this is best-effort read-check-write (NOT atomic — true locking needs the inner store's version predicate).
- **Test-gap (M126)** — Birko.Data.Patterns.Tests existed (Sluggable CR-H075 + RuleSpecification); augmented with the M124 counting-enumerable regression, `VersionedStoreWrapperTests` (create/increment/stale/missing-row, sync + async over the InMemory store), and `PagedResultTests` (TotalPages ceiling + HasNext/PreviousPage boundaries).
- Suite green: Birko.Data.Patterns.Tests 22. STORY-026 now **122/275**.

### Code-review remediation — medium findings, MongoDB.ViewModel + MongoDB.Views (EPIC-014 / STORY-026) (2026-07-09)
Twenty-first STORY-026 batch: **CR-M121 / M122 / M123** across Birko.Data.MongoDB.ViewModel + .MongoDB.Views (3 closed). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **ViewModel test-gap (M121)** — new **`Birko.Data.MongoDB.ViewModel.Tests`**: constructor store-type validation (accepts a plain `AsyncMongoDBStore` or a wrapper; a foreign store → `ArgumentException`) and the unwrapping `MongoDBStore` getter (a fake `IAsyncStore`+`IStoreWrapper` around an `AsyncMongoDBStore` unwraps via the property where the direct cast is null). Offline; registered in `.slnx` + `.code-workspace`.
- **Dead Auto-mode fallback (M122)** — `MongoViewStore.ExecutePipelineAsync`'s `catch (MongoCommandException) when (Auto)` never fired: aggregating a **non-existent** MongoDB view returns an empty cursor, not an exception, so Auto mode silently returned empty instead of falling back. Replaced with an explicit `ViewExistsAsync` (a `ListCollectionNamesAsync` name-filter check, mirroring `MongoViewManager.ExistsAsync`) — Auto falls through to on-the-fly when the view is absent; Persistent still queries it unconditionally. The dead catch is gone. Code-review verified (live-Mongo path).
- **Invalid SharedGUID (M123)** — the `.projitems` SharedGUID and `.shproj` ProjectGuid were `a1b2c3d4-e5f6-4a7b-8c9d-mgoview0001` (the final segment isn't hex, so not a legal GUID — shared-project tooling can choke). Replaced with a real GUID in both files; verified nothing else referenced the old value.
- Suites green: MongoDB.Views.Tests 6, MongoDB.ViewModel.Tests 4. STORY-026 now **119/275**.

### Code-review remediation — medium findings, Birko.Data.MongoDB store (EPIC-014 / STORY-026) (2026-07-09)
Twentieth STORY-026 batch: **CR-M117 … M120** across the Birko.Data.MongoDB store (4 closed). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Read overrides bypassed lazy-init (M117/M118)** — `MongoDBStore` overrode the public `Read(Guid)` + parameterless `Read()`, and `AsyncMongoDBStore` overrode `ReadAsync(Guid)`, going straight to `Collection.Find` — skipping the base wrappers' `EnsureInitialized`/`EnsureInitializedAsync` gate (which also observes the `CancellationToken`), against the "override `*Core`, not public CRUD" convention. All three were also redundant (the base routes through the existing `ReadCore`/`ReadCoreAsync` single + bulk overrides). Deleted them.
- **Save upsert skipped init (M119)** — `AsyncMongoDBStore.SaveAsync`'s update/upsert branch ran the native `ReplaceOneAsync(IsUpsert: true)` without `EnsureInitializedAsync` (only the create branch was gated, via `CreateAsync`). Added `await EnsureInitializedAsync(ct)` at the top of the branch.
- **Store test-gap (M120)** — the `.Tests` covered only ChangeStreamOptions / AggregationPipelineBuilder; augmented with `MongoSettingsAndIndexTests`: `Settings.GetConnectionString` assembly (credentials, `authSource`, `replicaSet`, `tls`, `retryWrites/Reads`; username-without-password omits creds), `GetId`, and `MongoDBIndexManager.ValidateScope` (made `internal`). Change-stream document-key mapping is already covered by `ChangeStreamDocumentKeyResolverTests`; store CRUD/bulk + the UnitOfWork live paths stay integration-tier.
- MongoDB's `InitCoreAsync` is a no-op today so the runtime impact of M117–M119 is latent, but the fixes restore the init/cancellation contract for subclasses that add real init. Suite green: Birko.Data.MongoDB.Tests 42. STORY-026 now **116/275**.

### Code-review remediation — medium findings, Migrations RavenDB + TimescaleDB (EPIC-014 / STORY-026) (2026-07-09)
Nineteenth STORY-026 batch: **CR-M114 / M116** — closes the Migrations-backend cluster bar the deferred async findings. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **RavenDB RQL injection (M114)** — `RavenDBDataMigrator.ParseFilterToRql` and `UpdateDocuments` interpolated string values as `'{s}'` with no escaping, so a value containing a quote/backslash broke or altered the query. `ParseFilterToRql` now returns `(rql, parameters)` using `$pN` placeholders, and `UpdateDocuments`/`DeleteDocuments` populate `IndexQuery.QueryParameters` (patch values use `$uN`) — values are bound parameters, never inline literals. (The `QueryParameters` property is set via target-typed `new()` to avoid pinning the SDK's `Parameters` type name across RavenDB.Client versions.) Offline tests assert the placeholders, `$gt/$gte/$lt/$lte/$ne` mapping, and a quote-containing value staying a parameter.
- **TimescaleDB test-gap (M116)** — already resolved: Birko.Data.Migrations.TimescaleDB.Tests exists (since the audit) with `TimescaleDBMigrationSqlTests` asserting `BuildCompressionPolicySql` (opt-in segmentby / custom orderby) and `BuildContinuousAggregateSql` (no dangling GROUP BY comma) DDL — the CR-H070/H071 SQL-gen bugs are fixed and covered.
- Suites green: Migrations.RavenDB.Tests 11, Migrations.TimescaleDB.Tests 4. STORY-026 now **112/275**. The Migrations cluster is complete except **M101 / M108 / M109**, parked as the dedicated async/CancellationToken batch (interface change across ~7 backends + the InfluxDB sync-over-async & Flux-count that need a live server).

### Code-review remediation — medium findings, Migrations MongoDB + SQL (EPIC-014 / STORY-026) (2026-07-09)
Eighteenth STORY-026 batch: **CR-M112 / M113 / M115** across Birko.Data.Migrations.MongoDB + .Migrations.SQL (3 closed). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Mongo CopyData dropped its transform (M112)** — `MongoDataMigrator.CopyData(source, target, transformJson)` never referenced `transformJson`; the pipeline was hard-coded to a single `$merge`, so a migration author's `$project`/`$addFields` produced a silent straight copy. Extracted `internal BuildCopyPipeline(target, transformJson)` that prepends the caller's transform — a single `{...}` stage or a `[...]` array of stages (parsed via a wrap trick since `BsonDocument.Parse` is object-only) — before the `$merge`. Offline-tested (no-transform → just `$merge`; single/array prepended in order).
- **Mongo test-gap (M113)** — Birko.Data.Migrations.MongoDB.Tests existed (context tests); augmented with the data-migrator pure logic (`BuildCopyPipeline` + `ParseFilter` empty/JSON/invalid). Store record-keeping + the transaction-joined live path stay integration-tier (Mongo2Go / replica set).
- **SQL test-gap (M115)** — the store/runner/schema-builder are already covered by since-the-audit **SQLite** tests (SqliteMigrationRunnerTests, SqlSchemaBuilderTests, CreateTablesMigrationTests — richer than the audit's "settings + MigrationResult only"); the remaining gap, `SqlDataMigrator.ParseFilterToWhere`, is now covered: `$gt/$gte/$lt/$lte/$ne` → SQL operator mapping, quoted `"field"` identifiers + `@pN` parameters, multi-condition AND, and that values stay **bound parameters** (an injection-looking value never reaches the SQL text).
- Both `ParseFilterToWhere` (SQL) and `ParseFilter`/`BuildCopyPipeline` (Mongo) were made `internal` so the projitems-compiled test assemblies reach them directly. Suites green: Migrations.MongoDB.Tests 7, Migrations.SQL.Tests 24. STORY-026 now **110/275**; next the last two Migrations backends — RavenDB (M114 RQL parameterization) + TimescaleDB (M116 test-gap).

### Code-review remediation — medium findings, Migrations.InfluxDB (EPIC-014 / STORY-026) (2026-07-09)
Seventeenth STORY-026 batch: the **Birko.Data.Migrations.InfluxDB** sub-cluster — **CR-M110 / M111** closed, **CR-M108 / M109** deferred. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Field-type corruption (M110)** — `CopyData`/`BulkInsert` wrote every non-string field via `Convert.ToDouble(...)`, collapsing bool/int/long to double and **throwing** (`FormatException`) on `DateTime`/`byte[]` (aborting a migration mid-run), and dropped `_time` so copied series were re-stamped to write time. Extracted an `internal ApplyValue(point, key, value)` that branches on the runtime type (string→tag; bool / integer / floating-point → the matching `PointData.Field` overload; null skipped; anything else → a string field, never throws); `CopyData` preserves the original timestamp via `record.GetTimeInDateTime()` + `.Timestamp(..., Ns)`. Offline-tested through `ToLineProtocol` (bool stays `true`, integers keep the `i` suffix, DateTime degrades without throwing).
- **Delete-predicate injection (M111)** — `ConvertFilterToFluxPredicate` returned the raw `filterJson` verbatim, so a JSON filter (`{"x":1}`) was sent as an InfluxDB delete predicate (which must be a Flux boolean expression) — silently matching nothing/everything; and `RemoveMigration` interpolated `migration.Name` unescaped. Now rejects JSON with a clear `NotSupportedException` (a real Flux predicate still passes through), and a new `EscapeFluxString` backslash-escapes the interpolated name. Offline-tested.
- **Deferred (M108/M109)** — M108 (sync-over-async throughout) is structural: the `IDataMigrator` interface is synchronous while the InfluxDB SDK is async-only, so it needs the shared migration interfaces to gain async members (the **CR-M101** CancellationToken/async work) rather than a blind per-backend rewrite. M109 (Flux `count()` over-counts multi-field measurements) needs a live InfluxDB to tune safely. Both parked for the dedicated async batch.
- Suite green: Migrations.InfluxDB.Tests 17. STORY-026 now **107/275**.

### Code-review remediation — medium findings, Migrations cluster (EPIC-014 / STORY-026) (2026-07-09)
Sixteenth STORY-026 batch: **CR-M102 … CR-M107** across Birko.Data.Migrations (core) + .Migrations.CosmosDB + .Migrations.ElasticSearch (6 closed; CR-M101 deferred). All three test projects already existed (verify-first). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Core runner tests (M102)** — Migrations.Tests augmented with `MigrationRunnerLogicTests`: RegisterMigrations duplicate-version rejection + sort, Migrate (target==current no-op / target<current failure) and Rollback (symmetric) guards, GetPending/AppliedMigrations partitioning, EnsureInitialized (Migrate-before-Initialize throws), and `GetMigrationsToExecute` Up/Down range selection incl. the **Down boundary** (`m.Version <= from && m.Version > to`) the finding flagged as off-by-one-prone — all via a fake `IMigrationStore` + a concrete `AbstractMigrationRunner` subclass.
- **CosmosDB rename swallowed everything (M103)** — `CosmosDBSchemaBuilder.RenameField`'s bare `catch {}` (meant to skip docs lacking the old field) also swallowed throttling (429), auth and service errors → a partial/failed rename reported success. Now catches only `CosmosException` with `BadRequest`/`NotFound`; everything else propagates.
- **CosmosDB filter identifier interpolation (M104)** — `CosmosDBDataMigrator.ParseFilterToSql` interpolated field names raw as `c.{name}` (values were escaped, identifiers weren't) → bracket-quoted `c["{name}"]` with escaping. `ParseFilterToSql` is `internal static`, so the CosmosDB.Tests assertions were updated to the bracket-quoted output + a special-char/embedded-quote injection case.
- **ES applied-versions cap (M105)** — `ElasticSearchMigrationStore.GetAppliedVersions` used `Size(1000)` + **Ascending**, silently truncating past 1000 and dropping exactly the highest versions — so `GetCurrentVersion()` (which takes `Max()`) under-reported and the runner re-ran applied migrations. Now **Descending** + `Size(10000)` (default `index.max_result_window`; beyond that needs scroll, noted inline).
- **ES record not refreshed (M106)** — `RecordMigration`/`RemoveMigration` didn't refresh, so a same-process read right after (a second `Migrate()`/`GetPendingMigrations()`) could miss the just-recorded version and re-execute it. Added `.Refresh(Elasticsearch.Net.Refresh.True)` to both (the async variants delegate to sync).
- **ES test-gap (M107)** — Birko.Data.Migrations.ElasticSearch.Tests already exists with `PainlessSourceTests` (the pure Painless-source builder — the bug-prone logic); the store round-trip stays an integration-tier concern.
- **Deferred (M101)** — threading `CancellationToken` through `InitializeAsync`/`MigrateAsync`/`RollbackAsync` + `IMigrationStore` ripples across ~7 backend implementers; its own batch.
- M103/M105/M106 are live-backend paths (code-review verified). Suites green: Migrations.Tests 14, Migrations.CosmosDB.Tests 11, Migrations.ElasticSearch.Tests 2. STORY-026 now **105/275**.

### Code-review remediation — medium findings, InfluxDB cluster (EPIC-014 / STORY-026) (2026-07-09)
Fifteenth STORY-026 batch: **CR-M094 … CR-M097** across Birko.Data.InfluxDB + .InfluxDB.ViewModel (4 closed). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **SaveAsync consistency (M094)** — `AsyncInfluxDBStore.SaveAsync`'s update branch wrote the point directly via `Client.GetWriteApiAsync().WritePointAsync(...)`, skipping `EnsureInitializedAsync` (a Save-update on a not-yet-initialized store could target a missing bucket) and `ExecuteWithRetryAsync` (lost the transient-failure retry every other CRUD path has). It now delegates to `UpdateAsync`, mirroring the create branch's use of `CreateAsync`.
- **Thin store tests (M095)** — InfluxDB.Tests expanded 9 → 25 over the pure-logic surfaces: `Settings.IsTransientException`, `FormatFluxInterval` (made `internal` — TimeSpan + "N unit" forms + fallback), `ModelToPoint` (measurement/Guid-tag/field emission via a test subclass + `ToLineProtocol`), and the `InfluxDbUnitOfWork` state machine (Begin activates, double-Begin → `TransactionAlreadyActiveException`, Commit/Rollback-without-active → `NoActiveTransactionException`, post-Dispose → `ObjectDisposedException`). `MapRecordToModel` (needs a `FluxRecord`) and live CRUD stay an integration-tier concern.
- **Double-destroy (M096)** — the `Destroy`/`DestroyAsync` overrides on both `AsyncInfluxDBRepository` and `InfluxDBRepository` called `base` (which already destroys the store) **and** `DropAsync`/`Drop` (the same unwrapped store) → the InfluxDB bucket was dropped an extra time per Destroy. Removed the overrides; the distinct public `DropAsync`/`Drop` remain for an explicit drop. (Aside surfaced while fixing: the base bulk repo destroys via both `Store` and `BulkStore` — the same instance — so it already destroys twice; that's a separate pre-existing base concern, not this finding.)
- **Tests (M097)** — new **`Birko.Data.InfluxDB.ViewModel.Tests`** (5): ctor type-guard (wrong store → `ArgumentException`), `IsHealthy` false without a client, and the M096 **structural** regression (the overrides are no longer declared on the InfluxDB repos while `Drop`/`DropAsync` still are — chosen over a fragile destroy-count because the base's Store/BulkStore double-call would muddy a count). Registered in `.slnx` + `.code-workspace`.
- Suites green: InfluxDB.Tests 25, InfluxDB.ViewModel.Tests 5. STORY-026 now **99/275**.

### Code-review remediation — medium findings, EventSourcing docs + JSON + Localization (EPIC-014 / STORY-026) (2026-07-09)
Fourteenth STORY-026 batch: **CR-M093 / M098 / M099 / M100** — a low-friction set of scattered Data.* fixes, all offline-verified against existing test projects (no new projects). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **EventSourcing README (M093)** — the README's Usage/API sections documented a fictional surface (`EventStore<T>`/`AsyncEventStore<T>`, `EventSourcedRepository<T>`, `CreatedEvent<T>`/`UpdatedEvent<T>`/`DeletedEvent<T>`, `EventStream`, `EventSnapshot`, `repo.GetAtTime(...)`) — none of which exist; a consumer following it couldn't compile a line. Rewritten to the real wrapper/extension API (`IEvent`/`DomainEvent`, `IEventStore`/`IAsyncEventStore`, `IEventSourced`/`EventSourcedAggregate`, the four `*EventSourcingStoreWrapper` types with `Replay`/`ReplayAsync` + `GetHistory`/`GetHistoryAsync`, and `.WithEventSourcing(...)`), matching the accurate CLAUDE.md.
- **JSON bulk create (M098)** — the **sync** bulk `CreateCore` did `item.Guid = Guid.NewGuid()` (discarding a caller-assigned Guid) and `_items.Add(...)` (throws `ArgumentException` on a duplicate key), diverging from both the single-item path (`??=`) and the async-bulk path (indexer upsert). Now uses `item.Guid ??= Guid.NewGuid()` + `_items[item.Guid.Value] = item`. Regression tests cover preserve-supplied-Guid, assign-when-absent, and duplicate-Guid-upsert-without-throw.
- **Localization dead code (M099)** — `LocalizedOrderByHelper.GetNonLocalizedOrderBy` was an abandoned stub (a for-loop whose entire body was comments explaining it couldn't be implemented, always returning null in the mixed case) with no callers — the wrappers use `ReferencesLocalizedField` + a full in-memory sort. Deleted.
- **Localization double-enumeration (M100)** — the sync `LocalizedBulkStoreWrapper` and async `AsyncLocalizedBulkStoreWrapper` `Create/Update/Delete(IEnumerable<T>)` overloads passed `data` to the inner store then `foreach`-ed `data` again for translations, enumerating a lazy/one-shot source twice (re-run or exhausted). All six now materialize once (`data as IList<T> ?? data.ToList()`). A counting-enumerable regression test asserts a single enumeration through sync Create and async CreateAsync.
- Suites green: JSON.Tests 14, Localization.Tests 70. STORY-026 now **95/275**; the InfluxDB cluster (CR-M094 … M097) is the next batch (needs a new `.InfluxDB.ViewModel.Tests`).

### Code-review remediation — medium findings, ElasticSearch cluster (EPIC-014 / STORY-026) (2026-07-09)
Thirteenth STORY-026 batch: **CR-M088 … CR-M092** across Birko.Data.ElasticSearch (+ .ViewModel, .Views) — 4 closed, 1 deferred. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Broken marker methods (M088)** — `EnumerableExtensions.MultiMatch`/`MoreLikeThis` tried to `Compile()` a `MemberExpression` that references a lambda parameter (always throws at runtime), but they are query-DSL **markers** recognized by name in `ElasticSearch.ParseExpression` and translated to Nest queries. Kept as markers with **throw-only bodies** (`NotSupportedException`, clear message) + class doc, so filter expressions that reference them still compile but a stray direct call fails loudly.
- **Wrapped-store unwrapping (M090)** — `ElasticSearchRepository.Count`/`ClearCache` resolved the store via `(Store as ElasticSearchStore<T>)`, which is **null for a wrapped store** (tenant wrapper) → Count silently returned 0 and ClearCache no-op'd. Both now go through the existing unwrapping `ElasticSearchStore` property (`GetUnwrappedStore<…>()`) that the class already used elsewhere. Fixed in **both** the `.ViewModel` repo and the reference `Birko.Data.ElasticSearch` repo (they shared the copy-pasted bug).
- **View index-name agreement (M091)** — the view **store** used `definition.Name` only in Persistent mode while the **manager** used it whenever present, so a named Auto/OnTheFly view had the manager ensuring/checking one index while the store queried another. Extracted a shared `ElasticSearchViewIndexResolver.Resolve(ViewDefinition)` (Persistent+Name → name; else `PrimarySource.Name`) and pointed both at it.
- **Tests (M092)** — new **`Birko.Data.ElasticSearch.ViewModel.Tests`** (M090: a minimal `IStore`+`IStoreWrapper` around an `ElasticSearchStore` proves the wrapper unwraps via the property where the direct cast is null; Count/ClearCache themselves hit a live cluster so the mechanism is what's asserted); ES.Tests gained a marker-throws test (M088); ES.Views.Tests gained resolver tests (M091). Suites green: ES.Tests 78, ES.Views.Tests 7, ES.ViewModel.Tests 3.
- **Deferred:** **M089** (ES store CRUD/`BulkAsync`/`ReadStreamAsync` scroll paging/`AggregateAsync` test-gap) left **open** — needs a mocked/Testcontainers `ElasticClient` tier, out of scope for the pure-logic batches (noted in the audit rather than falsely closed).
- STORY-026 now **91/275**.

### Code-review remediation — medium findings, CosmosDB cluster (EPIC-014 / STORY-026) (2026-07-09)
Twelfth STORY-026 batch: **CR-M084 … CR-M087** across Birko.Data.CosmosDB + Birko.Data.CosmosDB.Views (4 findings). All closed; each bug verified via pure-logic unit tests (no live emulator). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Aggregation SQL identifiers (M084)** — `CosmosAggregationHelper.BuildAggregateSqlParts` interpolated model property names as dotted `c.Field` accessors (brittle vs a camelCase serializer, and a field name with special chars could break/inject SQL). Now emits **bracket-quoted** `c["Field"]` via an `internal FieldRef` helper (embedded `"`/`\` escaped) for the SELECT group-by columns, aggregate source fields, and the GROUP BY clause. The view store's WHERE/ORDER BY (separate `TranslateFieldAccess`) were untouched, so the existing CR-H044/H045 tests still pass.
- **Index path normalization (M085)** — `CosmosDBIndexManager.CreateAsync` persisted a single-field index as `/name/?`, but `ExistsAsync`/`DropAsync`/`GetInfoAsync` compared against the **raw** name (so an index created by name was never found), and `DropAsync` unconditionally pushed the raw name into `ExcludedPaths` even when nothing was removed. Extracted shared `NormalizeIncludedPath`/`NormalizeFieldPath`/`IncludedPathMatches`/`FieldPathMatches`/`PolicyContainsIndex`/`RemoveIncludedIndex` helpers; all four operations now agree, and Drop mirrors only the path it actually removed.
- **Filter value translation (M086)** — `CosmosFilterTranslator.TranslateValue` fell back to a raw `ToString()` for `DateTime` (culture-dependent, unquoted → invalid SQL) and enums (unquoted member name). Now `DateTime`/`DateTimeOffset` are ISO-8601 **quoted** (matching System.Text.Json's default) and enums emit their **numeric** value; string escaping already used the correct Cosmos backslash form.
- **Tests (M087)** — the (since-the-audit) `Birko.Data.CosmosDB.Views.Tests` was augmented with `CosmosViewTranslateValueTests`; `Birko.Data.CosmosDB.Tests` gained `CosmosAggregationHelperTests` + `CosmosDBIndexManagerPathTests`. Both helpers/translator are `internal` and reachable because these projects **compile the `.projitems` source directly** (no assembly boundary → no `InternalsVisibleTo`, no reflection needed). Suites green: CosmosDB.Tests 43, CosmosDB.Views.Tests 7.
- STORY-026 now **87/275**; next up the remaining Data.* storage-backend findings (CR-M088 …: ElasticSearch, EventSourcing, InfluxDB, JSON, Localization, Migrations.*, …).

### Code-review remediation — medium findings, core foundation cluster (EPIC-014 / STORY-026) (2026-07-09)
Eleventh STORY-026 batch: **CR-M076 … CR-M083** across the foundational non-backend projects — Birko.Configuration, Birko.Contracts, Birko.CQRS, Birko.Data.Composition, Birko.Data.Core (8 findings). All closed. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **3 confirmed bugs fixed (verify-first):** **M076** `Settings.LoadFrom(ISettings)` did an unchecked `(Settings)data` cast → threw `InvalidCastException` for any foreign `ISettings`; now type-guarded (`if (data is Settings s) LoadFrom(s)`), foreign impls are a safe no-op. **M078** `RetryPolicy.GetDelay` cast `(long)Math.Pow(mult, attempt-1)` which overflowed to `long.MinValue` (a large **negative** TimeSpan that slipped past the `> MaxDelay` clamp, then `Task.Delay` throws) around attempt 53+; now computes the scaled delay in `double` and saturates at `MaxDelay` before casting. **M082** `AbstractModel.CopyTo()` / `AbstractLogModel.CopyTo()` returned `clone!` (i.e. **null**) when called with no argument, violating the non-null `ICopyable<T>` contract (`model.CopyTo().Guid` → NRE); now guard-clause `if (clone == null) return this;`.
- **Already resolved (verify-first):** **M077** `Birko.Configuration.Tests` and **M080** `Birko.CQRS.Tests` already existed (created since the audit); M080's covariant-dispatch regression (`SendAsync<object?>` of an `IQuery<string?>` must not poison the (requestType,resultType)-keyed handler cache — the CR-H039 fix) was already covered. Configuration.Tests augmented with `GetId` (each level) + `LoadFrom(ISettings)` foreign/Settings/null tests.
- **3 new test projects:** **`Birko.Contracts.Tests`** (10 — RetryPolicy: fixed/exponential/saturation, the M078 overflow boundary at attempt 53/100/1000/`int.MaxValue`, +-25% jitter), **`Birko.Data.Core.Tests`** (17 — `ExpressionParameterReplacer` AndAlso/OrElse incl. null-left + single-parameter shape, `ModelByGuid`/`ModelsByGuid` incl. null-Guids→null-Filter, `CopyTo` null-handling, `ViewModel.RaisePropertyChanged`), **`Birko.Data.Composition.Tests`** (8 — `StoreWrapperBuilder.Build<T>` over the InMemory store: plain→raw, per-marker wrapping, context-gating skip for audit/event-sourcing, Default→SoftDelete→raw chain order via `IStoreWrapper.GetInnerStore`, and the EventSourcing ctor-arity guard). Registered in `.slnx` + `.code-workspace`.
- Suites green: Contracts 10, Configuration 11, Data.Core 17, Composition 8. STORY-026 now **83/275**; next up the Data.* storage-backend medium findings (CR-M084 …).

### Code-review remediation — medium findings, WebSocket (Communication cluster complete) (EPIC-014 / STORY-026) (2026-07-09)
Tenth STORY-026 batch: **CR-M073 … CR-M075** (WebSocket) — which closes the **entire Communication cluster** (CR-M036 … CR-M075, 40 findings across Bluetooth, Camera, GraphQL, gRPC, Hardware, IR, Modbus, Network, NFC, OAuth, REST, REST.Server, SOAP, SSE, WebSocket). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Sync-over-async (M073)** — `WebSocketPort` Write/Open/Close/ReadWorker blocked on async WebSocket I/O via `.Wait()`/`.Result`, wrapping exceptions in `AggregateException` (so the rethrow surfaced the wrong type) and risking a captured-context deadlock. All four now use `ConfigureAwait(false).GetAwaiter().GetResult()`.
- **Missing IDisposable (M074)** — `WebSocketAuthenticationService` owned an `AuthenticationService` (a `ReaderWriterLockSlim` holder) and had a `Dispose()`, but didn't implement `IDisposable`, so the DI container never called it and the lock leaked for the container's lifetime. Now `: IDisposable`.
- **Tests (M075)** — `Birko.Communication.WebSocket.Tests` expanded 9 → 33: `WebSocketServer` start/stop/**restart** lifecycle + `IsListening` + idempotent `Stop` + `BroadcastAsync` guards (bound to a real loopback `HttpListener` on an OS-assigned free port, with bounded waits — no flaky fixed ports), and `WebSocketAuthenticationService` token validate/extract reject-vs-allow + the middleware decision. Buffer ops were already covered.
- STORY-026 now **75/275**; Communication cluster done.

### Code-review remediation — medium findings, SSE cluster (EPIC-014 / STORY-026) (2026-07-09)
Ninth STORY-026 batch: **CR-M067 … CR-M072** — the whole `Birko.Communication.SSE` cluster (6 findings, all bugs). All closed. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **SseClient was a no-op stub (M071, the big one)** — `ReceiveLoop` only `Task.Delay`-looped, `ProcessEventLine` (fully implemented) was never called, and `ConnectAsync` reported connected without opening anything. Implemented real HTTP SSE streaming: an injectable `HttpClient`, `ConnectAsync` opens a streaming GET (`ResponseHeadersRead`, `Accept: text/event-stream`, forwards headers + `Last-Event-ID`, `EnsureSuccessStatusCode`), and `ReceiveLoop` reads the stream line-by-line into `ProcessEventLine` so `OnMessage`/`OnEvent`/`LastEventId`/`retry` actually fire, with `AutoReconnect` re-opening via `Last-Event-ID`. A mock-handler test streams events and asserts the callbacks.
- **ConnectAsync leaked on reconnect (M070)** — a repeat call now tears down the prior session (CTS + receive task) before starting a new one instead of orphaning them.
- **Rate-limit leak (M067)** — `SseRateLimitMiddleware` pruned only the current key's list and never removed empty keys, so `_connectionHistory` grew one entry per distinct client IP forever (a slow leak/DoS in the abuse-mitigation component). It now prunes every client's history and removes fully-aged-out keys each request.
- **Denial reason dropped (M068)** — `SseResponse.Denied(status, reason)` discarded `reason`; added a `Reason` property so denials carry diagnostic text (origin-not-allowed / rate-limit / auth error).
- **Query-token parsing (M069)** — `ExtractTokenFromQueryString` now `TrimStart('?')`s and splits each pair with `Split('=', 2)`, so a raw query with a leading `?` or a token value containing `=` (base64 padding) no longer causes spurious 401s.
- **Malformed keep-alive comment (M072)** — `CreateComment` smuggled `": text"` through `Data`, so `ToString` emitted `data: : text` (a data field, corrupting the stream). Comments are now a distinct `SseEvent.Comment` property serialized as a real `: text` line.
- SSE.Tests → 19. STORY-026 now **72/275**.

### Code-review remediation — medium findings, REST / REST.Server / SOAP (EPIC-014 / STORY-026) (2026-07-09)
Eighth STORY-026 batch: **CR-M059 … CR-M066** across the web-protocol client/servers (8 findings). All closed. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **REST client credentials (M059)** — `RestClient.Credentials` was a plain auto-property read nowhere, so setting it silently did nothing. It is now backed by the client's own `HttpClientHandler` (get/set route through `handler.Credentials`, HttpClient owns the handler so Dispose covers it), and actually authenticates requests. Regression test reflects the handler to assert the credentials reach it.
- **REST sync shims + docs (M060/M061)** — the synchronous `Get/Post/…` overloads are documented as convenience shims over the async methods (a class-level `<remarks>` steers callers to the `*Async` overloads — the only ones with a `CancellationToken`). CLAUDE.md was rewritten to the *real* thin `RestClient` surface (raw-string returns, int-ms `Timeout`, `DefaultHeaders`, sync+async verbs) — the fictional `AsyncRestClient`/`RestRequest`/`RestResponse`/`GetData<T>`/`RestException`/retry/cache API it previously documented is gone (README already matched reality).
- **REST.Server (M062/M063)** — `RestAuthenticationService` declared a `Dispose()` (disposing the inner `AuthenticationService`'s `ReaderWriterLockSlim`) but not `: IDisposable`, so `using`/generic-dispose never called it and the lock leaked; it now implements the interface. REST.Server.Tests exists (error-factory JSON escaping) + the IDisposable regression.
- **SOAP (M064/M065/M066)** — the static `SoapClient` cache is now a `ConcurrentDictionary` with `GetOrAdd` (was a `Dictionary` with an unlocked check-then-act `GetClient` that could double-construct or throw on a duplicate `Add`); `RemoveClient`/`ClearCache` use `TryRemove`. `SoapServer` gained a synchronous `Stop()` core so `Dispose()` calls it directly instead of `StopAsync().GetAwaiter().GetResult()` (removing the sync-over-async + a noise `await Task.CompletedTask`). SOAP.Tests exists + concurrency/server regressions.
- Suites green: REST 19, REST.Server 7, SOAP 7. STORY-026 now **66/275**.

### Code-review remediation — medium findings, Communication test-gaps + NFC polling (EPIC-014 / STORY-026) (2026-07-09)
Seventh STORY-026 batch: **CR-M051 (IR), M056/M057 (NFC), M058 (OAuth)** — one real bug + three test-gaps. All closed. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **NFC silent polling death (M056)** — added an `INfcTransport.PollingError` event (`EventHandler<Exception>`); `SerialNfcTransport`, `HttpNfcTransport` and `HidNfcTransport` now wrap the background poll loop in try/catch — a non-cancellation fault (reader unplug, timeout, `JsonException`, mid-poll close) is surfaced via `PollingError` and stops the loop instead of silently faulting the detached task. Each transport also tracks its poll `Task` so `StopPollingAsync` can await completion (Http keeps `HttpRequestException` transient but surfaces other faults). Regression tests drive a malformed body + a thrown fault and assert `PollingError` fires (bounded by a timeout so a silent-death regression fails rather than hangs).
- **NFC tests (M057)** — `NfcReaderPort` (lifecycle, transport→port event forwarding, `ReadData` population, protocol-application pipeline, APDU passthrough, dispose-unsubscribe), `SerialNfcTransport.ParseResponse`/`DetectTagType` (PN532 frames, via reflection — both private static), and `HttpNfcTransport` (status/tag/apdu/204/ownsClient) against a mocked handler. NFC.Tests → 121.
- **IR tests (M051)** — `SamsungAcProfile` (codebook, 16/30 temp clamp, checksum, unknown-name null, 14-byte frame), `InfraredPort` (Send*Async delegation, `Write` `%4` `ArgumentException`, `HandleReceivedTiming` protocol-match vs raw-fallback), `HttpIrTransport` (signed-duration mapping, `StartReceiveAsync` `NotSupportedException`, ownsHttpClient disposal), `SerialIrTransport.ProcessReceivedLine`. RC5 round-trip + RawProtocol were already covered. IR.Tests → 102.
- **OAuth tests (M058)** — `PollDeviceTokenAsync` (pending→success, `slow_down` interval bump, `expired_token`, timeout), the real `refresh_token` grant path + `GetTokenAsync` refresh-then-fallback, `ExchangeCodeAsync` confidential-client (`client_secret`, no `code_verifier`), and a 401-retry test whose handler inspects the genuinely re-sent (cloned) request. OAuth.Tests → 54.
- STORY-026 now **58/275**.

### Code-review remediation — medium findings, Communication ports/protocols (EPIC-014 / STORY-026) (2026-07-09)
Sixth STORY-026 batch: **CR-M047 … CR-M055** across gRPC, Hardware, Modbus, Network (8 closed; CR-M051/IR deferred to the next test-gap batch). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **gRPC auth interceptor (M047)** — `WithAuth` copied into a new `Metadata` instead of mutating the caller-supplied one; reused `CallOptions`/`Metadata` no longer accumulate duplicate `authorization`/`x-tenant` entries. Regression test asserts one header per call + caller instance untouched.
- **Hardware `Serial` (M048/M049)** — constructor now guard-throws `ArgumentNullException` on null settings (the `null!` `SerialPort` field was a latent NRE in every method); all `ReadData` access (`Read`/`HasReadData`/`RemoveReadData`/`Clear`/`ReadSerial`) is serialized on a dedicated `_readLock` (was only `ReadSerial`, locking the `SerialPort`), and `RemoveReadData` now copies-and-removes atomically — which also fixes the latent `RemoveRange(0, -1)` crash for `size < 0`.
- **Modbus TCP (M052)** — the MBAP transaction id is now threaded into `WaitAndParseResponse` and validated against the response; a mismatch throws `IOException` rather than accepting a stale/out-of-order frame. (Fixed a pre-existing test that relied on a static-txId mock — it now echoes each request's txId.)
- **Network `Udp` (M054)** — `Write` guards `_remoteEndPoint` alongside `_client` (CS8604 + latent NRE). M053 (RemoveReadData TOCTOU) was already atomic under `lock(ReadData)` (CR-H026/H027) — not reproducible.
- **New test projects** — `Birko.Communication.Hardware.Tests` (M050, 23: GetID for Serial/Infraport/LPT + buffer logic + null guard) and `Birko.Communication.Network.Tests` (M055, 25: TcpIp/Udp GetID + buffer edges), both hardware-free (ports never opened). Suites green: gRPC 25, Modbus 71, Hardware 23, Network 25. STORY-026 now **54/275**.

### Code-review remediation — medium findings, Communication.Camera + .GraphQL (EPIC-014 / STORY-026) (2026-07-09)
Fifth STORY-026 batch: **CR-M043 … CR-M046** (Camera ×2, GraphQL ×2). All closed; audit `Status` flipped to `done`. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Camera — orphaned ffmpeg (M043)** — `FfmpegCameraSource.CaptureFrameAsync` now kills the process tree in a `finally` when it survives a cancellation/failure (`Process.Dispose()` does not terminate a running child), so a cancelled/failed capture no longer leaks an ffmpeg process holding the camera device. No-op on the success path.
- **Camera — argument injection (M044)** — the ffmpeg command line is now built with `ProcessStartInfo.ArgumentList` (per-argument tokens, extracted to an `internal BuildArguments` for testing) instead of one interpolated `Arguments` string; the Windows/macOS default device paths dropped their shell-style quotes. Tests assert a space-containing device name stays a single unquoted token and a malicious `DevicePath` can't inject extra args.
- **GraphQL — request serialization (M045)** — removed the `SemaphoreSlim(1,1)` `_requestLock` that funneled every caller of a shared (process-wide, per-endpoint) client into strictly sequential HTTP round-trips. `HttpClient` is thread-safe and the client holds no per-request mutable state. A regression test drives two concurrent requests through a gating handler and asserts both are in flight at once.
- **GraphQL — subscription dispatch untested (M046)** — extracted the per-frame handling from `StartReceivingAsync` into `internal HandleMessageAsync(string)` and unit-tested the `next`→OnNext, wrong-`id`→filtered, `complete`→OnCompleted+stop, `error`→OnError+stop, and next-with-payload-errors→OnError paths (frame reassembly incl. >8KB was already covered by `WebSocketMessageReaderTests`).
- Suites green: Camera 15, GraphQL 58. STORY-026 now **46/275**.

### Code-review remediation — medium findings, Communication.Bluetooth cluster (EPIC-014 / STORY-026) (2026-07-09)
Fourth STORY-026 batch: the **Communication.Bluetooth cluster CR-M036 … CR-M042** (7 findings). All closed; audit `Status` flipped to `done`. Mostly platform-gated (`#if WINDOWS`/`#if LINUX`) code, so verification was mixed — the buffer logic is platform-agnostic and tested, the Linux branch was compile-checked with `DefineConstants=LINUX`, and the WinRT branch is code-review-only (not compiled off a Windows TFM). [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **`Read()` TOCTOU (M036)** — the availability check and `GetRange` now run under one `lock(ReadData)`, so the background read worker can't change the count between them. Platform-agnostic; regression tests added.
- **Linux discovery leaks (M037)** — the timeout `CancellationTokenSource`, the linked CTS, and the `bluetoothctl` `Process` are now all wrapped in `using`.
- **Reconnect (M038)** — a `_reconnecting` guard stops `Open()` from resetting `_reconnectAttempts` during a reconnect, so `MaxReconnectAttempts` actually bounds the retry chain (it was effectively infinite because the success-path reset fought the increment). The remaining thread-supervision concern (reconnect runs on the dying read thread) is documented inline.
- **WinRT sync-over-async (M039)** — `OpenWindows` now checks `connectTask.Wait(timeout)` and throws `TimeoutException` on `false` instead of proceeding on a stale `_isOpen` and dropping the task's exception.
- **Docs (M040)** — CLAUDE.md described a fictional API (`BluetoothCommunicator`/`BLEServer`/`BLEService`/`BLEScanner` with `ConnectAsync`/`GetServiceAsync`); rewritten to the real surface (`Bluetooth`/`BluetoothSettings` VCP, `BluetoothLE`/`BluetoothLESettings` port, static `BluetoothLEDevices.DiscoverDevicesAsync`, `DiscoveredDevice`). README examples corrected (`SubscribeProcessData`/`RemoveReadData`; static discovery).
- **Placeholder stubs (M041)** — removed the dead RSSI-parsing branch; Linux service-filtered discovery now throws `NotSupportedException` rather than silently returning all devices (Windows placeholders left documented to avoid unverifiable behavior changes).
- **Tests (M042)** — `Birko.Communication.Bluetooth.Tests` already existed; augmented with `Read()` semantics + `GetID` formatting (12 hardware-free tests). STORY-026 now **42/275**.

### Code-review remediation — medium findings, Caching cluster (EPIC-014 / STORY-026) (2026-07-09)
Third STORY-026 batch: the **Caching cluster CR-M030 … CR-M035** (6 findings). All closed; audit `Status` flipped to `done`. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **MemoryCache stampede lock (CR-M030)** — the per-key `SemaphoreSlim` locks are now reference-counted (`KeyLock` with `Refs`/`Removed`) and retired by their last releaser; the cleanup timer no longer evicts locks. This closes the race where a lock evicted between `GetOrAdd` and `WaitAsync` let two callers run the factory concurrently. An acquire-retry loop handles the case where a caller grabs a lock another thread is retiring.
- **HybridCache write-through (CR-M031)** — `SetAsync`'s write-through branch now always awaits the L1 write in a `finally` (with the L2 call inside the `try` so a synchronous throw is caught too), so the L1 write is observed even when L2 faults with fallback disabled — no more orphaned fire-and-forget. Tests expanded (CR-M032): `L1MaxExpiration` capping (via `GetL1Options`), the `WriteThrough=false` branch + its no-fallback rethrow, and L2-failure fallback for `Remove`/`Exists`/`RemoveByPrefix`/`Clear`.
- **RedisCache cancellation (CR-M034)** — `Get`/`Set`/`Remove`/`Exists`/`RemoveByPrefix`/`Clear`/`GetOrSet` now call `ThrowIfCancellationRequested()` before touching Redis (and inside the `RemoveByPrefix` `KeysAsync` loop). Because the connection is lazy, offline regression tests assert a pre-cancelled token throws with no live Redis.
- **Already resolved / pre-existing:** CR-M033 (dead `KeyTimeToLiveAsync` round-trip) was already gone — `RefreshSlidingExpirationAsync` uses `ComputeRefreshedTtl` against the stored absolute deadline (fixed with CR-H014). CR-M035's `Birko.Caching.Redis.Tests` already existed (created since the audit); expanded with the cancellation tests.
- Caching suites green: Caching 25, Hybrid 34, Redis 12. STORY-026 now **35/275**.

### Code-review remediation — medium findings, BackgroundJobs cluster (EPIC-014 / STORY-026) (2026-07-08)
Second STORY-026 batch: the **BackgroundJobs cluster CR-M015 … CR-M029** (15 findings) across all 8 job-queue backends. Verify-first again — CosmosDB/SQL/Redis/XML test projects already existed. All 15 closed; audit `Status` flipped to `done`. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **Atomic dequeue claim** — the SQL backend's proven `ClaimToken` conditional-update + re-read-verify (CR-H011) was ported to the doc stores: **MongoDB** (CR-M020 — `$set` is per-document atomic), **RavenDB** (CR-M021 — re-read-verify picks a single winner even under last-write-wins), **ElasticSearch** (CR-M016 — best-effort; the ~1s refresh window is documented, handlers must be idempotent). A `ClaimToken` field was added to each doc model.
- **File backends** — JSON (CR-M018) and XML (CR-M029) `DequeueAsync` now serialize the read-claim-update with a `SemaphoreSlim` (mirroring `InMemoryJobQueue`); cross-process concurrency stays unsupported by design (documented in each CLAUDE.md).
- **Redis** — `GetByStatusAsync` now orders by `EnqueuedAt` **before** truncating to `limit` (was truncating an unordered set, CR-M023); every `IJobQueue` method and `RedisJobLockProvider` observe the `CancellationToken` (CR-M024); the dequeue claim (ZREM + hash flip to Processing) is now performed entirely inside the Lua `DequeueScript`, closing the orphan/double-dispatch window (CR-M025).
- **SQL lock provider** — the lock connection no longer leaks when `OpenAsync` throws (moved inside the try, CR-M026); non-PostgreSQL backends get provider-appropriate locking (MSSql `sp_getapplock`, MySQL `GET_LOCK`) or return `false` for SQLite/unknown instead of falsely reporting a lock was acquired (CR-M027).
- **Tests** — new `Birko.BackgroundJobs.{JSON,ElasticSearch,RavenDB,MongoDB}.Tests` (CR-M017/M019/M022 + M020 coverage), each with an in-memory store double (mirroring the CosmosDB pattern) covering lifecycle, FailAsync retry-vs-dead boundary, PurgeAsync, and atomic-claim distinctness; `Birko.BackgroundJobs.SQL.Tests` expanded with lock-provider (non-Postgres → false) + FailAsync-boundary tests (CR-M028). Registered in `.slnx` + `.code-workspace`. All 8 BackgroundJobs suites green (JSON 6, XML 7, Redis 7, SQL 17, CosmosDB 3, Mongo 6, ES 6, Raven 6). STORY-026 now **29/275**.

### Code-review remediation — medium findings, Birko.AI cluster (EPIC-014 / STORY-026) (2026-07-08)
Started STORY-026 (the 275 **medium** code-review findings) with the first batch: the **Birko.AI cluster CR-M001 … CR-M014** across `Birko.AI`, `.Agents`, `.Contracts`, `.Orchestration`, `.Providers`, `.Resilience`. These findings were unverified reviewer claims, so each was confirmed against the current code before fixing (verify-first). All 14 closed; audit `Status` flipped to `done`. [tasks/EPIC-014/STORY-026](tasks/EPIC-014-code-review-remediation/STORY-026-medium-findings/STORY.md).
- **`CancellationToken` threaded end-to-end (CR-M002/CR-M008):** added `CancellationToken cancellationToken = default` to `ILlmProvider.SendMessageAsync`/`SendMessageStreamingAsync` and threaded it through `LlmProviderBase` (retry `Task.Delay`s, `HttpClient.SendAsync`, `ReadAsStringAsync`, `[EnumeratorCancellation]` on the SSE parsers), **all 16 providers** (7 direct + `OpenAiCompatibleProviderBase` + its 8 subclasses + `TrackedLlmProvider`), the `Agent` run loop (`RunAsync`/`ContinueAsync` → sync/streaming loops, `ThrowIfCancellationRequested` per iteration), and `Tool.ExecuteAsync` (+ all 9 tools; `RunCommandTool` links the caller token into its timeout CTS). A genuine cancel now propagates instead of being swallowed by the timeout-only retry filter.
- **Resource leaks:** `SendWithRetryAsync` now disposes the buffered `HttpResponseMessage` on every return path — `StatusCode`/`IsSuccessStatusCode` stay readable post-dispose, so the 16 callers are unaffected (CR-M010). `LlmStreamingResponse` is now `IDisposable`/`IAsyncDisposable` (holds the streaming response as `Resource`), and the agent enumerates it under `await using`, so an abandoned stream (early exception/cancel) no longer leaks the connection (CR-M003).
- **Sync-over-async removed (CR-M001):** `Agent.HandleResponse` is now `async Task<(bool,bool)?>` and `await`s `HandleToolUse` — the `.GetAwaiter().GetResult()` bridge is gone.
- **Resilience:** `TrackedLlmProvider` now re-checks `CanMakeRequest` in a bounded loop (`WaitForRateLimitSlotAsync`, tunable `MaxRateLimitWaitAttempts`/`MaxSingleRateLimitWait`) and returns an error rather than proceeding blind after one delay (CR-M011). `ProviderRateLimiter.GetRetryAfter` computes backoff for token-per-minute and daily windows (max of active blocking windows), not just requests-per-minute (CR-M012). `ProviderCircuitBreaker` persistence is serialized per provider via an ordered task chain with an awaitable `FlushPersistenceAsync`, so a stale snapshot can't overwrite a newer one (CR-M013).
- **`AgentOptions` round-trip (CR-M007):** `ToDictionary`/`FromDictionary` now symmetric — `AllowedExternalPaths`, `EnableStreaming`, `StreamingFallbackToSync`, `CheckpointInterval` no longer silently reset to defaults.
- **Not reproducible:** CR-M006 (`Merge` already copies `OnLlmResponseReceived`; existing test covers it).
- **Tests:** new **`Birko.AI.Agents.Tests`** (CR-M005, 18 tests: `IsCodingAgent`, `AgentFactory` registration/aliases, depth-guidance bands, async run loop + cancellation) registered in `.slnx` + `.code-workspace`. `Birko.AI.Tests` / `.Orchestration.Tests` / `.Resilience.Tests` already existed (created since the audit — CR-M004/M009/M014 closed) and were expanded with regression tests for the fixes. AI-cluster suite now **59** (11 + 18 + 17 + 3 + 10), all green, zero warnings. Remaining medium findings CR-M015 … CR-M275 still to triage.
