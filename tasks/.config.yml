# tasks/.config.yml — configuration for the /tasks skill.
# Generated by /tasks import on 2026-05-28 from TODO.md. Safe to commit.

mode: local                    # local | hybrid

# This family commits straight to main — no task branches. See CLAUDE.md
# § Task tracking › Integration model for the polyrepo three-commit rule.
integration: single-branch     # pr-per-task | single-branch

# When mode: hybrid, fill in the external section:
# external:
#   provider: github             # github | jira
#   repo: owner/name             # github only
#   project: SUP                 # jira only (project key)
#   default-labels:
#     - agent-pickable
